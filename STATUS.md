# Status

## Frontend Dev

### 2026-06-29 — Responsive layout + navigation overhaul (width token, useViewport, mobile bottom-nav, desktop frame + 2-pane messages, Moderation moved to Profile)
- **One width system** — added `layout: { gutter: 20, maxContent: 640, maxForm: 400 }` to `src/tokens.js`. Replaced every per-screen content-column `maxWidth` (was 540/560/600/640) with `t.layout.maxContent` so there's no tab-to-tab width jitter: `App.jsx` (header inner + both verify banners), `SuggestionScreen`, `MatchesScreen`, `SafetyScreen`, `SettingsScreen`, `AdminScreen`, `ProfileScreen`, `messaging/MatchesListScreen` (×3), `messaging/BlockReportScreen`. `AuthScreen` routed through `t.layout.maxForm` (400); `OnboardingScreen` (480) and `LandingScreen` hero (760) left intentionally at their own widths. `UnmatchSheet` (540) left as a modal-sheet width (not a content shell).
- **Box-model guard** — `index.html` global `<style>` now has `*,*::before,*::after{box-sizing:border-box}`, `img,svg{max-width:100%}`, `body{overflow-x:hidden}`.
- **`src/useViewport.js`** — new hook returning `"mobile" | "tablet" | "desktop"` via `matchMedia` (desktop ≥1024, tablet 600–1023, else mobile), subscribed to media-query `change` events.
- **Navigation by device** — primary nav is ALWAYS the same 4 destinations (Discover · Matches · Messages · Profile), identical for admin and non-admin. On **mobile** the top tab bar is replaced by a FIXED bottom tab bar (`position:fixed; bottom:0`, 4 items ≥44px, icon+label, active=`t.accent`, `paddingBottom: env(safe-area-inset-bottom)`, `role=tablist/tab` + `aria-selected` + focus rings); `<main>` gets `paddingBottom:64` so content clears it. On **tablet/desktop** the existing top tab bar is kept. New `BottomNavTab` component + small inline nav glyphs in `App.jsx` (HeartIcon reused for Matches).
- **Moderation moved off the primary nav** — removed the admin-only "Moderation" top tab. `ProfileScreen` now takes `isAdmin` + `onOpenModeration` and renders a "⚙ Moderation" button near sign-out (new `ModerationButton`); App still renders `<AdminScreen/>` at `activeTab==='admin'`, now reached via Profile. Result: every user sees the identical 4-item nav. Admins can leave the admin view via the still-rendered top bar (desktop/tablet) or bottom bar (mobile).
- **Desktop/tablet frame** — on `!isMobile`, `<main>` becomes a centered surface "panel" (`background:t.surface; border:1px solid t.border; borderRadius:16; margin:24px auto`), capped at `maxContent+48` — reads as an app panel, not a strip adrift in grey. Mobile stays full-bleed.
- **Two-pane Messages on desktop** — `messaging/MessagingApp.jsx` reads `useViewport()`; on **desktop** it renders the conversation list (fixed 340px, `overflowY:auto`, right border) beside the open thread (flex) in a row, with the panel widened to 1080px. Empty thread pane shows a calm "Select a conversation" placeholder. Selected row is highlighted (`MatchesListScreen` threads a new optional `selectedConversationId` → `aria-current` + surfaceAlt bg); the thread's redundant "← Matches" back button is hidden via a new optional `hideBack` prop on `ConversationScreen`. Mobile/tablet keep the EXISTING list→conversation stack-swap unchanged.
- All hooks remain before any early return (`useViewport` added at the top of both `App` and `MessagingApp`; `hideBack` only gates JSX, not the `fBack` hook). `npm run build` clean (89 modules). Not deployed. ~Visual Dev

### 2026-06-29 — Animated "living logo" (AnimatedSpectrumMark)
- **New `src/AnimatedSpectrumMark.jsx`** — a calm animated version of the 6-tile brand mark. On mount the tiles **assemble** left→right: each tile fades + rises 6px + grows from `scale(0.86)`, staggered ~70ms apart, `t.motion.slow`(420ms)/`t.motion.gentle` easing, `animation-fill-mode: both` so each settles on the exact static `SpectrumMark` end-state (opacity 1, no transform). Opacity/transform-led, ≤6px travel — one-shot, never bouncy or looping-distracting. Same tile geometry/colours as `SpectrumMark` (square tiles, `height*0.16` gap, literal green→teal→sand hex). `transform-box: fill-box` + `transform-origin: center` so each tile scales in place. Props: `height` (default 18), `idle` (default **OFF**), `gap`, `radius`, `style`. Keyframes (`spectrumTileAssemble` + `spectrumTileBreath`) injected once via an id-guarded `<style>` (mirrors `Spectrum.jsx`'s loader pattern) — no animation libs.
- **Optional idle** (`idle` prop) — a *very* subtle slow brightness "breathing" sweep (`filter: brightness(1→1.06→1)`, ~6.5s `ease-in-out` infinite), staggered per-tile so it reads as a soft wave; starts only **after** the assembly fully settles (delayed by `assembleDone + i*stagger`) so entrance and idle never overlap. Default OFF; enabled only on the landing hero — a place where a quiet brand moment is appropriate. Header stays calm (idle OFF).
- **Reduced motion** — dynamic `usePrefersReduced` hook (same as `UnmatchSheet.jsx`/`MatchMoment.jsx`, with SSR/`matchMedia` guards). When reduced, the component returns the plain static `<SpectrumMark>` with zero motion (true static fallback, no chance of an unsettled frame). Belt-and-braces: even with the system pref off, App's global reduce-motion sheet (`animation-duration:0.001ms!important; animation-iteration-count:1!important`) collapses the keyframes to the settled `both`/end frames, so the mark renders fully visible and still.
- **Wired in** — replaced `<SpectrumMark height={14}/>` in the `App.jsx` header with `<AnimatedSpectrumMark height={14}/>` (idle OFF — header stays calm; removed the now-unused `SpectrumMark` import from App.jsx). Replaced the `LandingScreen.jsx` hero mark with `<AnimatedSpectrumMark height={36} idle/>` (larger + gentle idle for the brand moment); the serif "Spectrum" wordmark is unchanged. The landing **footer** keeps the static `SpectrumMark height={12}` (no need to animate a footer), so that import stays valid. `SpectrumMark.jsx` is untouched and remains the static fallback.
- `npm run build` clean (88 modules). Animated mark renders; reduced-motion shows the static mark. Not deployed. ~Brand Director

### 2026-06-29 — Round-3 QA: AllCaughtUp wired, empty-conversation illustration, error-state pattern + illustrations, favicon coverage, context-card removed from candidate cards
- **AllCaughtUp wired** — `src/SuggestionScreen.jsx` now imports `AllCaughtUp` and renders it (~110px) above the calm copy in the exhausted-deck / "No new suggestions right now." empty state (`atEnd` branch). The already-built illustration was previously exported but unused.
- **Empty-conversation illustration** — `src/messaging/EmptyConversationState.jsx` now imports `EmptyMessages` and places it (~96px) above the "You matched with {name}." heading, matching its sibling empty states. Starters logic untouched.
- **Illustration set completed** — `src/illustrations.jsx` gains two more soft 1.5px line-SVG glyphs in the existing style (`t.accent`/`t.clay`, no faces, `aria-hidden`): `GenericError` (a gently tilted rounded square with a quiet "!" resting inside) and `Offline` (a soft cloud with disconnected dashes drifting below).
- **Reusable `src/ErrorState.jsx`** — `ErrorState({ title = "Something went wrong", message, onRetry })`: centered, `role="alert"`, small `GenericError` (~80px) + serif title + `message` in `t.textSoft` + a secondary-variant `Button` "Try again" rendered only when `onRetry` is provided. Applied to the bare-text load-failure branches in: `src/MatchesScreen.jsx` (new `loadFailed` state + `loadMatches` retry; action-error inline `<p>` for CAP_REACHED etc. kept separate), `src/messaging/MatchesListScreen.jsx` (new `loadFailed`/`onRetry` props; `src/messaging/MessagingApp.jsx` now tracks `convsLoadFailed` and exposes a `retryLoadConversations` via `refreshKey` instead of swallowing the error), `src/messaging/ConversationScreen.jsx` (replaced the `apiError` one-liner; new `reloadKey` re-runs the load effect on retry), and `src/AdminScreen.jsx` (reports load-failure → `ErrorState` with `onRetry={refresh}`). No hook moved after an early return in any file.
- **Favicon coverage** — generated `public/favicon-32.png` + `public/favicon-16.png` from `public/icon.svg` (tile motif on `#3E6660`) via sharp. `index.html` now has `<link rel="icon" type="image/png" sizes="32x32"…>` + `sizes="16x16"`, keeps `<link rel="icon" href="/icon.svg">` as the modern default, and adds `<link rel="mask-icon" href="/icon.svg" color="#3E6660">` for Safari pinned tabs. (No `.ico` — PNG sizes + SVG cover the real cases.)
- **Context card removed from candidate cards** — `src/SuggestionScreen.jsx`: `CommStyleArea`/`commStyleChips` no longer render the free-text `contextCard` ("In their words" blockquote) to non-matched strangers (backend is being changed to stop sending it pre-match). The structured comms/sensory chips are kept. The `contextCard` still shows post-match (`MatchesScreen` / profile) — untouched. The queue still maps the field through harmlessly; nothing renders it pre-match.
- `npm run build` clean (87 modules); `favicon-32.png` + `favicon-16.png` present in `dist/`. Not deployed. ~Visual Dev

### 2026-06-29 — Signature "match moment" overlay (same-wavelength)
- **New `src/MatchMoment.jsx`** — a calm full-screen overlay for a NEW mutual match. Props `{ you:{name,userId,photoUrl}, them:{…}, onContinue, onOpenChat }`. Choreography is entirely opacity-led with short travel + soft deceleration (`t.motion.slow` / `t.motion.gentle`): (1) warm dim backdrop `rgba(36,51,45,0.55)` fades in; (2) two `<Avatar size={88}/>` (you left, them right) start ±24px apart (translateX only, no scale) and glide to a small gap; (3) the 6-colour brand spectrum ramp draws in left→right between them — each tile staggered ~60ms (base delay 220ms so the avatars have mostly met), fade + grow from 2px via `scale(0.25)→scale(1)`, like a connecting thread; (4) serif line **"You're on the same wavelength."** + subline "You both said yes." settle up (8px rise) at 360ms; (5) two reused `<Button>`s — **"Say hello"** (primary → `onOpenChat`) + **"Keep looking"** (tertiary → `onContinue`). NO confetti/bounce/sound. `entered` flips on next rAF to start the transitions; under reduced-motion (`usePrefersReduced`, same hook as UnmatchSheet) `entered` starts `true` so the END state renders immediately — avatars together, tiles drawn, text visible — with only the global fade, no movement.
- **A11y:** `role="dialog"` + `aria-modal` + `aria-labelledby` (heading) + `aria-describedby` (subline); heading is `tabIndex={-1}` and focused on open (announced); Escape → `onContinue`; Tab/Shift+Tab focus-trapped between the two buttons. Buttons keep their 44px targets + focus rings (Button's own ring); "Keep looking" text forced white for contrast on the dim backdrop.
- **`src/Button.jsx`** — wrapped in `forwardRef` (additive, ref forwarded to the underlying `<button>`) so MatchMoment's focus trap can reference the two actions. No existing call site changed; default export preserved.
- **Wired into `src/SuggestionScreen.jsx`** — imported `MatchMoment` + `getUserId`; new `getViewerIdentity()` helper builds `you` from the cached `spectrum_profile` (displayName/photoUrl) + `getUserId()`. New `matchId` state captured from the swipe result (`result.matchId`) alongside the existing `mutual` flag; reset in `next()`. Replaced the old in-card mutual-match interstitial with `<MatchMoment>` rendered as a fixed full-screen sibling when `stage === "confirmed" && mutual && lastPerson`: `them` = the just-liked candidate (displayName/memberId/photoUrl), `onOpenChat` advances past the moment then calls `onOpenMessages()` (lands them in Messages — the simplest existing pattern; `matchId` is captured for a future deep-link/createConversation flow), `onContinue` = `next()` (normal post-swipe advance). The non-mutual confirmed path and skip/not-now flows are untouched; `liveRef` is still used by the non-mutual block and its focus effect is null-guarded. ALL hooks remain before every early return — no hook moved.
- `npm run build` clean (86 modules). Not deployed. ~Visual Dev

### 2026-06-29 — Public landing page + calm empty-state illustrations
- **New `src/LandingScreen.jsx`** — the calm marketing front door (mobile-first, hero max-width ~760, sections centered). Hero: `<SpectrumMark height={32}/>` + "Spectrum" serif wordmark, headline "Dating at your own pace.", one-line subhead ("A calmer, clearer way to meet people — made with and for autistic adults."), and two buttons — **"Create your profile"** (primary → `onGetStarted`) + **"Sign in"** (tertiary → `onSignIn`). **"Why Spectrum is different"** — 4 value cards (white `t.surface`, soft `t.border`, `green50` icon chips) reusing `HeartIcon`/`GearIcon`/`ShieldIcon`/`SealCheckIcon` from `src/icons.jsx`: No pressure / Clarity over guesswork / Safety first / Built with the community. **"How it works"** — 3 steps (Make your profile → Discover people who fit → Talk when you're ready) each with a small 2×2 spectrum-tile motif (`StepTiles`, green/teal/clay) + number. **Closing CTA** — `green50` panel, repeats "Create your profile" with "Free to join. Leave whenever you like." **Footer** — muted mission line + "Your safety & privacy matter." note (no fake legal links/hotlines). All colours via `t.*` (auto-themes). Motion: a single one-shot fade on mount, neutralised by App's global reduce-motion stylesheet AND a scoped `@media (prefers-reduced-motion: reduce)` guard. Local `useFocusable` for focus rings; 52px primary/tertiary button targets.
- **`src/AuthScreen.jsx`** — added an optional `initialMode` prop (`"login"`|`"register"`, defaults `"login"`; seeds the existing `mode` state — login/register toggle behaviour untouched) and an optional `onBack` prop rendering a focusable "← Back" button above the wordmark (new `fBack` focusable). No behaviour regressed when props are omitted.
- **`src/App.jsx`** — new unauthenticated flow. Added `showAuth`/`authMode` state (declared with the other hooks, before any early return — no hook-after-return). A NEW visitor now sees `LandingScreen` first; `onGetStarted` → AuthScreen in **register** mode, `onSignIn` → AuthScreen in **sign-in** mode; AuthScreen's `onBack` returns to the landing page. Returning users with a stored token still skip straight to the app (the `authed` branch is unchanged and wins). Session-expiry routes to AuthScreen (login mode, `showAuth=true`) so the expiry message has context; sign-out and account-deletion reset `showAuth=false` to return to the calm landing page.
- **New `src/illustrations.jsx`** — 3 soft single-weight LINE SVG empty-state illustrations on transparent ground, `aria-hidden`, `size` prop (default 112), 1.5px stroke in `t.accent`/`t.clay` (CSS-var literals so they theme): `EmptyMatches` (two cups + steam), `EmptyMessages` (rounded speech shape + resting dots), `AllCaughtUp` (calm sun on a horizon). Wired `EmptyMatches` (size 104) into `src/MatchesScreen.jsx`'s empty state and `EmptyMessages` (size 104) into `src/messaging/MatchesListScreen.jsx`'s empty state, above the existing calm copy.
- WCAG 2.2 AA: decorative illustrations/marks `aria-hidden`; landing buttons keep ≥44px targets + visible focus rings; sections use semantic `header`/`section`/`ol`/`footer` with `aria-labelledby` headings; primary/tertiary use `t.accentStrong` for AA contrast; reduced-motion honoured. `npm run build` clean. Not deployed. ~Visual Dev

### 2026-06-29 — Visual polish (gradient-monogram avatars, skeleton loaders, button hierarchy)
- **Gradient-monogram avatars** — new shared `src/Avatar.jsx` exporting `Avatar({ name, userId, photoUrl, size=56, style })`. With a `photoUrl` it renders the image (circle, `object-fit: cover`); otherwise a deterministic two-tone diagonal gradient (`linear-gradient(135deg, …)`) derived from a stable string hash of the seed (`userId || name`) over a small palette of six calm brand pairs (greens→teals→sand/clay, literal hex so identical in both themes), with the initial centered in `t.serif`, white, `~size*0.42`. Visual is `aria-hidden` (surrounding context names the person). Adopted by replacing the per-screen monogram/avatar renderers: `SuggestionScreen.jsx` (removed local `Monogram`/`avatarBg`/palette; candidate lockup now `<Avatar userId={person.memberId} photoUrl size={88}/>`), `MatchesScreen.jsx` (removed local `Avatar`; `userId={otherUser.userId}`, size 56), `messaging/MatchesListScreen.jsx` (removed `SmallMonogram`/`avatarBg`/palette; `userId={otherUser.userId}`, size 44 via `AVATAR_SIZE`), and `ProfileScreen.jsx` (the photo-gallery EMPTY state now shows the member's own default gradient avatar at size 72 with calm "This is your default avatar…" copy — `PhotoGallery` takes a new `name={displayName}` prop). Photo-upload logic untouched — only the fallback visual changed.
- **Skeleton loaders** — new `src/Skeleton.jsx` exporting `Skeleton({ width, height, radius, style })`: a tinted block (`t.surfaceAlt` base, `t.green50` highlight) with a slow 2s shimmer via a one-time-injected `<style>` keyframe. Respects reduced motion — a `@media (prefers-reduced-motion: reduce)` rule drops the gradient + animation to a static tint. Replaced the "Loading…" text states with 2–3 calm skeleton rows/cards in `MatchesScreen.jsx` (`MatchesSkeleton`, 3 rows), `messaging/MatchesListScreen.jsx` (`MatchesListSkeleton`, 3 rows under the kept "Your matches" heading), and `AdminScreen.jsx` (`ReportsSkeleton`, 2 cards).
- **Button hierarchy** — new `src/Button.jsx` exporting `Button({ variant='primary', children, ...props })` with three calm variants: `primary` (solid `t.accentStrong`, white), `secondary` (`t.green100` fill, `t.accentStrong` text, 1px `t.border`), `tertiary` (transparent, `t.accentStrong` text). 44px min height, focus ring, 150ms ease transition; accepts `style` for width control (not forced full-width). Demonstrated in a few high-traffic spots only: `MatchesScreen.jsx` "Say hello"/"Open chat" action (primary / secondary by conversation state, removed its bespoke button), and `SafetyScreen.jsx` (← Back + elapsed-banner Dismiss as secondary, Share plan as primary). Did NOT refactor every button — the reusable component + these adoptions is the deliverable.
- WCAG AA: avatars/skeletons `aria-hidden`, decorative only; skeleton honours reduced-motion; Button keeps 44px targets + visible focus ring; all colors via `t.*` so both themes are covered. No hook moved after an early return. `npm run build` clean. Not deployed. ~Visual Dev

### 2026-06-29 — Visual identity phase 2 (warm dim theme via CSS variables + Settings theme toggle)
- **Approach — CSS variables, zero component churn.** Converted every COLOR token in `src/tokens.js` to reference a CSS custom property with the current light hex as the fallback, e.g. `bg: "var(--c-bg, #F4F5F2)"`, `text: "var(--c-text, #24332D)"`, `bgGradient: "var(--c-bg-gradient, linear-gradient(150deg, #F4F5F2 0%, #ECF0EB 100%))"`. Did this for all 32 color keys: bg/bg-gradient, surface, surfaceAlt, text, textSoft, textMuted, accent, accentStrong, positive, border, borderLight, focus, danger, warning, sand, clay, teal, the green50…green900 ramp, formBorder, bubbleOwn, bubbleOther, tombstone. `serif`/`sans` font strings left as literal strings (untouched). No key renamed or dropped — same keys, only values changed to `var(...)`. Components read `t.*` exactly as before and re-resolve live on theme switch.
- **`index.html` — both themes defined in the global `<style>`.** `:root` holds the LIGHT values, which EXACTLY match the current tokens.js hex (incl. the phase-1 nudged `--c-border:#D3DBD5`), so light mode is byte-for-byte unchanged. `:root[data-theme="dim"]` holds the warm-dim palette: deep warm-green surfaces (`--c-bg:#1C2422`, `--c-surface:#232D2A`, `--c-surfaceAlt:#283330`), lifted text (`--c-text:#E4EAE6`, `--c-textSoft:#B6C2BC`) and lifted accents (`--c-accent:#7FB0A7`, `--c-accentStrong:#8FBCB2`, `--c-positive:#7FB87A`, `--c-danger:#E08585`, `--c-warning:#D9B45A`) chosen to meet WCAG AA on the dim surfaces; the green ramp is inverted for dark (low end → dark panels, high end → light brand greens for text). `body { background }` now reads `var(--c-bg, …)`. Verified: identical 32-variable set in both themes; every var referenced in tokens.js is defined in both.
- **Theme persistence + live apply.** Extended the existing `spectrum_a11y` settings object with a `theme` field (`'light' | 'dim'`) rather than a separate key — keeps one settings blob. `readA11y()`/`DEFAULT_A11Y` in `SettingsScreen.jsx` updated to read/normalise it (defaults to `'light'`). `App.jsx`: new `applyTheme(prefs)` sets `document.documentElement.dataset.theme = prefs.theme === 'dim' ? 'dim' : ''`; called from both `applyA11y` (live, on toggle) and the existing `a11y` effect (covers initial mount + every change). The high-contrast `filter`/larger-text `zoom`/calm-background overrides are untouched and layer on top of either theme.
- **Settings UI.** Page H1 renamed "Accessibility" → "Settings" (the screen now covers both). New "Theme" section at the top: serif subhead + calm copy "Warm dim is easier on the eyes in low light." and a segmented control (`role="radiogroup"` / `role="radio"`, 44px targets, focus rings via `useFocusable`) with Light / Warm dim options; selecting one calls the same live-apply `update("theme", v)` path as the other settings. The four accessibility toggles sit under an "Accessibility" subhead below it, unchanged. No hook moved after an early return (this screen has a single return anyway).
- `npm run build` clean. Not deployed.

### 2026-06-29 — Visual identity phase 1 (Newsreader + Atkinson type, SVG icons, spectrum-tile mark, color-depth tokens)
- **Typography:** `index.html` now preconnects to Google Fonts and loads `Newsreader:opsz,wght@6..72,400;6..72,600;6..72,700` + `Atkinson+Hyperlegible:wght@400;700` (with `&display=swap`); added a global `body { font-family: 'Atkinson Hyperlegible', … }`. `src/tokens.js`: `serif` is now `'Newsreader', Georgia, 'Times New Roman', serif` and a new `sans: "'Atkinson Hyperlegible', -apple-system, Segoe UI, Roboto, sans-serif"` was ADDED. Replaced every hardcoded `fontFamily: "-apple-system, Segoe UI, Roboto, sans-serif"` (18 occurrences across 11 files) with `t.sans`. Headings already on `t.serif` pick up Newsreader automatically. The two files with their own LOCAL `t` objects (`OnboardingScreen.jsx`, `messaging/BlockReportScreen.jsx`) had `serif` updated + `sans` added locally so `t.sans` resolves there too. No token key removed — only values changed / keys added; verified every `t.sans` user has a `sans` source.
- **Iconography:** new `src/icons.jsx` exports small duotone-ish inline-SVG components (`ShieldIcon`, `GearIcon`, `HeartIcon`, `SealCheckIcon`) — 24-viewBox, 1.5px stroke, `currentColor`, low-opacity `currentColor` fill, `size` prop (default 18), `aria-hidden`. Replaced frontend emoji: `🛡`→`<ShieldIcon/>` and `⚙`→`<GearIcon/>` in `App.jsx` (Safety/Settings header links + the `⚙ Moderation` nav tab label, now `<><GearIcon/> Moderation</>`); `✓`→`<SealCheckIcon/>` in `VerifiedBadge.jsx`. (Reaction-picker emoji inside ConversationScreen and the push-payload emoji in the backend are out of scope.)
- **Spectrum-tile mark:** new `src/SpectrumMark.jsx` — decorative SVG of 6 rounded-rect tiles stepping green→teal→sand (`#5E9459 #4F8A8B #3E6660 #6FA39A #C9A875 #E7D9C4`), `height` prop (default 18, width auto), `aria-hidden`. Rendered at `height={14}` immediately before the "Spectrum" serif wordmark in the `App.jsx` header (wordmark kept). `public/icon.svg` updated: the plain serif "S" replaced with a centered row of 5 tiles in the same ramp on the `#3E6660` brand ground, keeping the 512 viewBox + `rx="112"` rounded corners (PNGs not regenerated — fine for now).
- **Color depth:** ADDED to `tokens.js` — `sand #E7D9C4`, `clay #C9A875`, `teal #4F8A8B`, and a coherent green ramp `green50 #EEF3F1 … green100 #DCE8E4, green200 #BCD4CC … green700 #3E6660 … green900 #24332D`. Nudged `border` from near-gray `#D8DDD6` to a subtly green-tinted `#D3DBD5` (mirrored in the two local-`t` files). Applied lightly: the quiet `SecondaryButton`s in `SafetyScreen.jsx` + `SettingsScreen.jsx` now use a `green100` fill instead of white; the Safety "What to say" script cards got a warm `sand` background + `clay` left-border accent. Conservative — the tokens being available is the main deliverable.
- `npm run build` clean. Not deployed.

### 2026-06-29 — Hinge-style profile prompts (editor + card display)
- `src/api.js`: added `getPromptCatalog()` → `GET /profile/prompt-catalog` (returns the `prompts` array, normalised) and `savePrompts(prompts)` → `PUT /profile/prompts` (returns the saved `prompts` array).
- **Item 1 — `src/ProfileScreen.jsx` (prompt editor):** new state `prompts` (`[{ promptKey, answer }]`, max 3), `promptCatalog` (`[{ key, text }]`), and `showPromptChooser` — all declared with the other `useState`s, BEFORE the loading/error early returns (no hook-after-return). A new own `useEffect` fetches `getPromptCatalog()` into `promptCatalog` (best-effort). The existing profile-load effect now maps `data.prompts` (server shape `{ promptKey, promptText, answer }`) into the editable `{ promptKey, answer }` shape, capped at 3. New calm **"Prompts"** card placed after "About you", before "Your interests", with the helper copy "Answer up to 3 prompts — an easy way to share who you are without a blank page." Each filled slot (`PromptSlot`) shows the prompt text + an editable answer textarea (≤200, live "remaining" counter) and a Remove control; an "Add a prompt" dashed button (`AddPromptButton`, shown when <3) opens a chooser (`PromptChooser`) — a `<select>` of catalog prompts NOT already chosen, then an answer textarea (≤200, counter), with Add/Cancel. `handleSave` now also calls `savePrompts(prompts.filter(p => p.promptKey && p.answer.trim()))` after `updateProfile` (best-effort; on failure surfaces "Your profile saved, but your prompts couldn't be saved."). Prompts are always saveable (the main Save button only gates on name + interests), so no extra `isDirty` plumbing was needed.
- **Item 2 — `src/SuggestionScreen.jsx` (candidate cards):** carried `prompts` through the candidate queue mapping (`c.prompts || []`). New `PromptCards` component renders each prompt Hinge-style — the `promptText` in a small muted uppercase label with the `answer` prominent in serif (20px) below — in its own quiet card after "How I communicate". Only rendered when the candidate has at least one answered prompt.
- **Item 3 — `src/MatchesScreen.jsx` (light):** when `otherUser.prompts` has an answered first prompt AND the row isn't already filled by a tagline/context card, show that first prompt's label + answer (single truncated serif line) as a calm preview under the name.
- WCAG 2.2 AA: answer textareas are labelled (visually-hidden label tied to the prompt in the editor) + `aria-describedby` to a polite live counter; Remove buttons carry a descriptive `aria-label`; the chooser select + answer are labelled; `useFocusable` focus rings and 44px targets throughout; prompt cards use semantic `ul`/`li`; colour is never the sole signal.
- `npm run build` clean. Not deployed.

### 2026-06-29 — Sensory / comms-style / context-card profile UI + candidate card display
- Surfaced the seven new "moat" profile dimensions (`commDirectness`, `commLiteral`, `commCadence`, `sensoryEnvironment`, `sensoryLighting`, `socialDuration`, `contextCard`) — they ride on the existing `getProfile`/`updateProfile` and appear on candidates + `match.otherUser`.
- **Item 1 — `src/ProfileScreen.jsx` (profile editing, end-to-end):** added all seven to `DEFAULT_PROFILE`; new `useState`s declared with the other hooks BEFORE the loading/error early returns (no hook-after-return — incl. a `contextCardTouched` flag for the counter). Populated from `data.*` in the load effect (each `|| ''`), wired into both `isDirty` branches + the effect dep array, the `savedProfile` snapshot, and the `updateProfile({...})` save payload. Two new calm cards placed after "Lifestyle", before "Notifications": (1) **"How you communicate"** — three `LifestyleSelect` dropdowns (Directness / Style / Reply pace) + the **"How to talk to me" context card** textarea (max 300, live "remaining" counter, helper copy as specified); (2) **"Sensory & environment"** — three `LifestyleSelect`s (Preferred setting / Lighting / Social energy). Reused the existing `LifestyleSelect` and textarea/counter patterns; helper "This helps matches know how to talk with you. Optional."
- **Item 2 — `src/SuggestionScreen.jsx` (candidate cards):** carried the seven fields through the candidate queue mapping (`c.* || ''`). New `CommStyleArea` + `commStyleChips(person)` helper render a tidy, low-stimulation "How I communicate" area in its own quiet `surfaceAlt` card (after Interests/Why): a calm row of labelled chips for the set comms/sensory prefs (e.g. "Quiet settings", "Direct", "Replies once a day"), intentionally skipping the low-signal `either`/`whenever`/`medium` values to keep 2–4 chips. When `contextCard` is set, a short quoted `blockquote` under a small "In their words" label. The whole area renders only when something is set.
- **Item 3 — `src/MatchesScreen.jsx` (light):** when `otherUser.contextCard` is present, show it briefly as a single italic quoted line (truncated with ellipsis) under the tagline.
- WCAG 2.2 AA: selects keep `aria-describedby`; the context-card textarea is labelled + `aria-describedby` (hint + polite live counter); candidate chips are a labelled `role="list"`; colour is never the sole signal.
- `npm run build` clean. Not deployed.

### 2026-06-29 — Accessibility settings (reduce motion, high contrast, larger text, calm mode)
- New `src/SettingsScreen.jsx` — frontend-only, no backend. Same calm shell as `MatchesScreen`/`SafetyScreen` (`t.bgGradient`, max-width 600, serif H1 "Accessibility", `← Back` via `onBack` prop). Four labelled `role="switch"` toggles (reusing the `ProfileScreen` switch pattern: `aria-checked` + `aria-labelledby`), each with a one-line plain description. Exports `A11Y_KEY` (`spectrum_a11y`), `DEFAULT_A11Y`, and a `readA11y()` helper that reads + normalises the saved JSON to a fully-typed object (safe against missing/garbage values). Changes save to localStorage immediately and call the passed `onChange(next)` so App re-applies live. Calm mode also flips reduce-motion on (kept coherent so the switches reflect what's applied). CRITICAL: all hooks (`useState` lazy-init, mount-focus `useEffect`, `update` `useCallback`) declared before the single return — no hook-after-return.
- `src/App.jsx` low-risk global application (no `tokens.js` refactor):
  - New `a11y` state read once on mount via `readA11y()`; `applyA11y(prefs)` callback updates state + localStorage + re-applies the stylesheet; an effect re-applies on every `a11y` change.
  - **Reduce motion / calm** → inject/remove a single `<style id="a11y-overrides">` in `document.head` with `*, *::before, *::after { animation-duration: 0.001ms !important; animation-iteration-count: 1 !important; transition-duration: 0.001ms !important; scroll-behavior: auto !important; }` (helper `applyA11yStylesheet`, idempotent; removes the node when neither pref is on).
  - **High contrast / larger text / calm background** → inline style overrides on the top-level app container via `a11yWrapperStyle(prefs)`: `filter: contrast(1.15)` (global, safe), `zoom: 1.15` (enlarges proportionally — px inline styles won't scale via root font-size; fixed sheets/dialogs render relative to the zoomed container so they stay consistent), and flatten `background` to `t.bg` (drops the decorative gradient) in calm mode. Footer note points users to browser/device zoom if they need more.
  - Discreet "⚙ Settings" header link (new `SettingsLink` mirroring `SafetyLink`: focusable, 44px, `aria-label="Accessibility settings"`/`aria-current`) sits in a flex row beside "🛡 Safety" — visible on all main tabs, NOT a 5th nav tab. New `activeTab === "settings"` branch renders `<SettingsScreen onBack={() => setActiveTab(prevTab || "suggestions")} onChange={applyA11y} />`; link sets `prevTab` on navigate-in so Back returns sensibly. `<main>` aria-label ternary now includes "Accessibility settings".
- WCAG 2.2 AA: `useFocusable` focus rings, 44px targets, `role="switch"`/`aria-checked`/`aria-labelledby` on every toggle, plain-language descriptions; respects the user's explicit choice regardless of OS reduce-motion.
- `npm run build` clean. Not deployed.

### 2026-06-29 — Pause toggle (#8), undo last skip (#7), privacy reassurance (#9), your-reports status (#10)
- `src/api.js`: added `undoSkip()` → `POST /matching/undo-skip` and `getMyReports()` → `GET /messaging/my-reports` (normalises `{ reports: [...] }` or bare array). `paused` rides on the existing `getProfile`/`updateProfile`.
- **#8 Pause/snooze** (`src/ProfileScreen.jsx`): new `paused` state declared with the other `useState`s — BEFORE the loading/error early returns (no hook-after-return). Added to `DEFAULT_PROFILE`, populated from `data.paused` in the load effect, wired into both `isDirty` branches + dep array, the `savedProfile` snapshot, and the `updateProfile({...})` save payload. New calm "Pause my profile" card (after Notifications, before Identity verification) reusing the existing switch pattern via a new `PauseToggle` (`role="switch"`, `aria-checked`, `aria-labelledby`). When ON, shows the reassuring literal copy "Your profile is paused. You won't appear in Discover… Your matches and messages stay."
- **#7 Undo last skip** (`src/SuggestionScreen.jsx`): extracted the candidate fetch/map into a reusable `loadCandidates` `useCallback` (also resets `index` to 0); mount effect now calls it. New `undoing` state + `handleUndo()` calls `undoSkip()`, and on `ok` re-fetches the deck so the un-skipped person reappears; `ok:false` and network errors resolve quietly (just resume viewing). Added an "Undo" `ActionButton` beside "Next person" on the confirmation interstitial, shown only after a `not_now`/`skip` choice ("Undoing…" while in flight).
- **#9 Privacy reassurance** (`src/SafetyScreen.jsx`): new calm "Your privacy" section advertising the no-presence design ("We never show when you're online… never put on the spot to reply quickly.").
- **#10 Your reports** (`src/SafetyScreen.jsx`): `getMyReports()` fetched on mount via a properly-declared effect (`reports`/`reportsLoading`/`reportsError` state, mount-guarded with an `active` flag). New "Your reports" section: per-report `reportedName`, `reason`, formatted date, and a `StatusPill` (Open=warning, Reviewed=accent, Actioned=accentStrong, Dismissed=textMuted). Empty state "You haven't reported anyone."; reassurance note "When you report someone, our team reviews it. You'll see the status update here."; loading + error states.
- WCAG 2.2 AA throughout: `useFocusable` focus rings, 44px targets, `role="switch"`/`aria-checked`/`aria-labelledby` on toggles, status pills carry their meaning as text (colour is not the sole signal).
- `npm run build` clean.

### 2026-06-29 — Verified badge across cards + profile verification section
- New reusable `src/VerifiedBadge.jsx` — a small, calm "✓ Verified" pill (12px, `t.positive` text + border, transparent fill, `title`/`aria-label="Identity verified"`, accepts an optional `style` override). Exported for reuse.
- Badge shown next to the display name (only when the relevant `verified` is true) in: `src/SuggestionScreen.jsx` (candidate name lockup — `verified` now carried through the queue mapping from `c.verified`), `src/MatchesScreen.jsx` (match card — `match.otherUser.verified`), `src/messaging/MatchesListScreen.jsx` (conversation row — `otherUser.verified`, name wrapped so the badge doesn't get truncated), and `src/ProfileScreen.jsx` (next to the "Your profile" heading).
- `src/ProfileScreen.jsx`: new `verified` state declared with the other `useState`s — BEFORE the loading/error early returns (no hook-after-return) — populated from `data.verified` in the API load effect. New calm "Identity verification" card (after Notifications, before Save): if verified → "✓ Your identity is verified." (positive); else → literal "coming soon" explainer + a disabled-looking "Coming soon" tag, no dead button.
- WCAG 2.2 AA: badge is subtle, non-flashy, `t.positive`; decorative ✓ marked `aria-hidden`, meaning carried by `aria-label`.
- `npm run build` clean.

### 2026-06-29 — Safety Center (meeting tips, copy-able scripts, date-plan share, check-in timer)
- New `src/SafetyScreen.jsx` — entirely client-side, no backend calls. Same shell styling as `MatchesScreen.jsx` (`t.bgGradient`, max-width 600, serif H1 "Safety Center"). Five sections: (1) **Meeting safely** calm plain-bullet checklist; (2) **What to say** — five autism-friendly literal scripts (break / clarity / ending early / declining / sensory) each in a quoted card with a "Copy" button (`navigator.clipboard.writeText` + execCommand fallback, "Copied" confirmation via shared `aria-live` polite region); (3) **Share a date plan** — form (who / where / datetime-local / "check by" time) that builds a plain-text summary and calls `navigator.share({ text })` if available, else copies it ("Copied — paste it to a trusted person"); (4) **Check-in timer** — 1h/2h/3h presets plus a "by HH:MM" preset when the plan's check-by time is set; persists `{ endsAt }` in localStorage (`spectrum_safety_checkin`) so it survives reload; live `setInterval` countdown (cleared on unmount), "Cancel timer", and on-elapse a calm in-app `role="alert"` banner + a `new Notification(...)` only if permission is *already* granted (never prompts); (5) **If you need help** — generic non-region-specific note, no fabricated numbers. Copied the small `useFocusable` hook locally. CRITICAL: every hook (including the lazy-init localStorage state and all effects) is declared before any early return — no hook-after-return.
- `src/App.jsx`: discreet "🛡 Safety" header link (new `SafetyLink`, focusable, 44px, `aria-label`/`aria-current`) added in a flex row beside the "Spectrum" wordmark — visible on all main tabs, NOT a 5th nav tab. New `activeTab === "safety"` branch renders `<SafetyScreen onBack={() => setActiveTab(prevTab || "suggestions")} />`; link sets `prevTab` on navigate-in so Back returns sensibly. `<main>` aria-label ternary now includes "Safety Center". `ProfileScreen.jsx` untouched.
- `npm run build` clean.

### 2026-06-29 — Lifestyle attributes + deal-breaker toggles
- `src/ProfileScreen.jsx`: added six new fields end-to-end. State (declared with the other `useState`s, before the loading/error early returns — no hook-after-return): `wantsChildren` / `smoking` / `drinking` (strings, default `''`) and `dbWantsChildren` / `dbNonSmoker` / `dbMustBeLocal` (booleans, default `false`). Extended `DEFAULT_PROFILE`, the API load effect (maps `data.wantsChildren` etc., default `''`/`false`), the `isDirty` comparison + `savedProfile` snapshot, and the `updateProfile({...})` save payload to include all six.
- New "Lifestyle" card (after "About your search", before "Notifications"): three calm labelled `<select>`s — Do you want children? (Prefer not to say / Yes / No / Open to it), Smoking and Drinking (Prefer not to say / No / Sometimes / Yes) — each with optional/“shown on your profile” helper text. New reusable `LifestyleSelect` sub-component.
- "Deal-breakers" subsection within the same card: three `role="switch"` toggles (new `DealBreakerToggle`, reusing the existing `NotificationToggle` switch pattern) for `dbWantsChildren` / `dbNonSmoker` / `dbMustBeLocal`, with literal copy and the calm explainer "Deal-breakers hide people who clearly don't match. People who haven't said yet still show up."
- WCAG 2.2 AA: focus rings via `useFocusable`, 44px targets, `aria-checked` / `aria-labelledby` on switches, `aria-describedby` on selects.
- `npm run build` clean.

### 2026-06-29 — DOB onboarding gate, age on cards, contact-gating copy
- `src/OnboardingScreen.jsx`: added required **Date of birth** field to step 1 — native `<input type="date">`, labelled, helper "You must be 18 or older to use Spectrum Dating.", `max` attr = today minus 18 years. Client-side step-1 validation: required, parses/validates the date, computes age, blocks advancing with inline error "You must be 18 or older to use Spectrum Dating." when < 18. `dateOfBirth` ('YYYY-MM-DD') added to the final `updateProfile(...)` payload; server's 400 message already surfaces via existing `handleSave` (`e.message`). New pure helpers `maxDobToday()` / `ageFromDob()` (browser-side, not in a workflow). All hooks remain before the single return — no hook-after-return.
- `src/SuggestionScreen.jsx`: candidate name now renders age when present ("Name, 29"), guarded by `typeof person.age === "number"`.
- Contact-gating clarity (audit #6): reassurance line on onboarding final step ("You're in control of who can reach you…"); calm one-liner added to empty states in `src/MatchesScreen.jsx` and `src/messaging/MatchesListScreen.jsx` ("Only people you've both matched with can message you.").
- `npm run build` clean.

### 2026-06-29 — Profile photo gallery (up to 6, choose main)
- Added photo API helpers to `src/api.js` (`addProfilePhoto`, `setPrimaryPhoto`, `deleteProfilePhoto`), each returning the server's ordered `photos` array.
- Replaced the single-avatar `PhotoUpload` in `src/ProfileScreen.jsx` with a responsive `PhotoGallery` (3-col grid, up to 6 cells). Each photo: square cover image, "Main" badge on the primary, "Set as main" on non-primary, inline-confirmed "Remove" (danger outline). One "+ Add photo" tile when under 6; helper text + empty state. Friendly 503 handling ("Photo uploads aren't available right now").
- `photos` state populated from `GET /profile/me` (`data.photos`); old `photoUrl` state and `PhotoUpload` component removed. All hooks declared before the loading/error early returns.
- `npm run build` clean.

### 2026-06-29 — Admin moderation dashboard + report wiring
- Added moderation/admin API helpers to `src/api.js` (`getAdminMe`, `getAdminReports`, `resolveReport`, `suspendUser`, `getAdminStats`, `reportUser`).
- New `src/AdminScreen.jsx`: self-contained moderation dashboard — stats row, status-filter segmented control (Open / Reviewed / Actioned / Dismissed / All), report cards with resolve (Reviewed/Actioned/Dismissed + optional note) and inline-confirmed suspend/unsuspend. Loading + empty states, WCAG 2.2 AA focus rings, 44px targets, live regions.
- `src/App.jsx`: captures `isAdmin` from login response and `getProfile()` (persists across reload), resets it on sign-out / expiry / account deletion. Discreet "⚙ Moderation" nav tab rendered only for admins; non-admins never see it.
- Report wiring: messaging `BlockReportScreen` submit now also calls `reportUser(..., conversationId)` alongside the existing block; pre-match `ReportModal` in `SuggestionScreen.jsx` now calls `reportUser` (plus block so the candidate is hidden).
- `npm run build` clean.

~Frontend Dev

### 2026-06-29 — Visual round-2: motion tokens, Spectrum primitive, drift-island fix, OG/favicon/maskable
- **Motion language → tokens.** Added a `motion` object to `src/tokens.js` (`fast`/`base`/`slow` durations; `standard`/`exit`/`gentle` easings). Adopted it where transitions already existed: `src/Button.jsx` (background/color/border-color → `base standard`), `src/LandingScreen.jsx` (both CTAs), and every switch/segmented control in `src/SettingsScreen.jsx` + `src/ProfileScreen.jsx` (track → `base standard`, thumb travel → `base gentle`). No per-use reduce-motion gate needed — App.jsx's global `transition-duration:0.001ms !important` sheet neutralises them all; confirmed unbroken.
- **Spectrum primitive.** New `src/Spectrum.jsx` exporting `Spectrum({ variant, value, count, ... })` built from the 6-colour brand ramp (green→teal→sand). Variants: `progress` (lit tiles + 12% unlit), `loader` (3 staggered cross-fading tiles, ~1400ms loop, static under reduce-motion), `meter`, `divider`. Decorative by default (`aria-hidden`); accepts `aria-label`. Wired `variant="progress"` into `src/OnboardingScreen.jsx` as the step indicator (replaced the bare "Step N of 3" text — kept the SR live region + step logic). Wired `variant="loader"` into the MatchesScreen "Starting…" button state.
- **Drift islands (dim-mode bug).** Removed per-component hardcoded `const t = {…}` shadows (with `serif:"Georgia…"` and frozen light hex that never themed) from `src/messaging/EmptyConversationState.jsx` (the named bug), plus `src/OnboardingScreen.jsx`, `src/AuthScreen.jsx`, `src/messaging/UnmatchSheet.jsx`, `src/messaging/BlockReportScreen.jsx` — all now `import { t } from "tokens"`. Fixed a leftover literal `#D8DDD6` border → `t.border` in AuthScreen. Georgia now appears only in the legitimate `tokens.js` font-stack fallback. Left intentional literals untouched (Avatar gradients, index.html theme vars, `#fff`, shadow rgba, brand tile hex). NOTE: a few pre-existing token-equivalent literals remain in `ConversationScreen.jsx` loading/error one-liners (`#4E5F58`, `#B94040`) and error-panel bg literals (`#FDF2F2`, `#FFF5F5`) — out of this pass's scope.
- **OG / favicon / maskable.** New `scripts/gen-share-assets.mjs` (sharp, Windows paths) generates `public/og.png` (1200×630, tiles + serif "Spectrum" + tagline on brand bg gradient) and `public/icon-maskable-512.png` (tile motif in ~80% safe zone on `#3E6660`). `index.html`: added og:title/description/image/type/url, twitter:card=summary_large_image + twitter:title/description/image, and `<link rel="icon" href="/icon.svg">` (apple-touch-icon kept). `manifest.json`: existing PNG icons → `"purpose":"any"`, added the maskable PNG as `"purpose":"maskable"` (fixes Android clipping from the old `"any maskable"`).
- `npm run build` clean; og.png + icon-maskable-512.png present in `dist/`; manifest valid JSON.

~Visual Dev

## Accessibility Director

### 2026-06-30 — A11y audit (keyboard/focus, ARIA, contrast in both themes, motion, forms, landmarks, autism calm-by-design)

Standard: WCAG 2.2 AA + autism calm-by-design. Method: full source audit of `index.html`, `src/tokens.js`, and all core-flow + messaging components; contrast ratios computed numerically for every flagged pair (both themes). NOTE: live-site + dev-server tooling (Chrome MCP, Preview) were both permission-denied this run, so findings are from code + computed ratios, not runtime captures — visual focus-order spot-checks on the deployed site still owed and listed at the end.

**Headline: the warm-dim theme regresses contrast on every filled "primary" control.** The dim palette deliberately makes `accentStrong` (#8FBCB2) and `positive` (#7FB87A) *light* tints — they're meant as TEXT on dark surfaces (they pass 6–7:1 there). But many controls use them as a FILL behind WHITE text. White-on-#8FBCB2 = **2.10:1**, white-on-#7FB87A = **2.32:1** — both fail AA badly. The `accentFill`/`dangerFill` tokens were created to fix exactly this and the messaging layer adopted them, but the rest of the app did not. Light theme is unaffected (accentStrong #3E6660 = 6.41:1). Since default theme is light, this bites only users who opt into Warm dim — but that's the low-vision/photophobia/sensory cohort most likely to need it, so it's serious.

🔴 Blocker
- **Primary buttons unreadable in dim theme** · 1.4.3 Contrast · `src/Button.jsx:13` (`primary` variant, white on `t.accentStrong`), `src/LandingScreen.jsx:59` (`PrimaryButton`), `src/SafetyScreen.jsx:73` (`PrimaryButton`), `src/OnboardingScreen.jsx:850` (Continue/Save). White-on-#8FBCB2 = 2.10:1 in dim. Fix: introduce a `t.onAccentFill`-style usage — switch these fills from `accentStrong` to `accentFill` (dim #356962 → white = passes), or define a dedicated "button fill" token that is dark in BOTH themes. One Button.jsx change fixes the largest surface (MatchesScreen "Say hello", MatchMoment "Say hello").
- **"I'm interested" action button unreadable in dim** · 1.4.3 · `src/SuggestionScreen.jsx:73` (`interested` kind: white on `t.positive`). 2.32:1 in dim. This is the single most important button in the core Discover→match flow. Fix: use a dark-in-both-themes positive fill (add a `positiveFill` token mirroring `accentFill`, or render dark text on the light tint).
- **"Yes, unmatch" destructive button low-contrast in dim** · 1.4.3 · `src/messaging/UnmatchSheet.jsx:177` (`background: t.danger`, white text). Dim `danger` #E08585 + white = 2.67:1. `dangerFill` (#9E3B3B → white = 6.68:1) already exists for this; DeleteConfirmDialog uses it correctly — UnmatchSheet was missed. Fix: `t.danger` → `t.dangerFill` on the confirm button (line 177–178 background+border).

🟠 Serious
- **Selected interest chips white-on-tint in dim** · 1.4.3 · `src/OnboardingScreen.jsx:154` & `:393`, `src/SuggestionScreen.jsx:114` (shared-interest pill). White on `t.accentStrong` = 2.10:1 in dim; these carry real text. Fix: same accentFill swap, or dark text on the tint.
- **Loading/error text hardcoded `#4E5F58` — invisible in dim** · 1.4.3 + theming regression · `src/SuggestionScreen.jsx:637` and `:645` ("Finding people for you…" / load-error). #4E5F58 on dim bg #1C2422 = 2.34:1. These literals survived the drift-island cleanup. Fix: `t.textSoft`.
- **Consent-gate + attachment-error panels hardcoded `#FFF5F5`/`#FDF2F2`** · 1.4.3 · `src/messaging/ConversationScreen.jsx:1402` (consent alert bg) and `:1433` (attachment error bg). In dim these stay near-white and `t.danger` (#E08585) text on them = 2.50:1; also visually jarring (a white box in a dark UI). Fix: theme the panel bg (e.g. `t.surfaceAlt`) and use `t.danger` per theme, or a tokenized danger-surface.
- **Disabled "Sign in" button hardcoded `#4E5F58`** · 1.4.3 / theming · `src/AuthScreen.jsx:246` (`loading ? "#4E5F58"`). White text on it = 6.77:1 so it *reads*, but it won't theme (stays the same grey-green in dim) and is an un-tokenized literal. Fix: a tokenized disabled fill.
- **ReportModal "Submit report" hardcoded `#B94040`** · theming · `src/SuggestionScreen.jsx:402`. White on it = 5.43:1 (passes), but the literal won't shift to `dangerFill` in dim — inconsistent with the rest. Fix: `t.dangerFill`.

🟡 Moderate
- **`aria-current="true"` is an invalid token** · 4.1.2 Name/Role/Value · `src/messaging/MatchesListScreen.jsx:76` (selected conversation row). `aria-current` takes an enumerated token; `"true"` is technically allowed by the spec but the semantically correct value here is `"page"` (matches the nav pattern used everywhere else in App.jsx). Low impact but should be `"page"` for consistency and correct SR phrasing.
- **VerifiedBadge text fails AA in LIGHT theme** · 1.4.3 · `src/VerifiedBadge.jsx:20` (`t.positive` text, #5E9459 on white = 3.59:1; on surfaceAlt = 3.15:1). It's a real word ("Verified"), not pure decoration. Dim is fine (6.11:1). Fix: use `accentStrong`/a darker green for the label text in light, or treat the green as icon-only + darker text.
- **Desktop 2-pane: empty thread placeholder isn't programmatically tied to the list** · 3.2.3/predictability · `src/messaging/MessagingApp.jsx:182–197`. The right pane shows "Select a conversation to start reading." as plain muted text with no role; when a keyboard user activates a list row, focus moves into the thread via ConversationScreen's heading focus — good — but the empty placeholder itself isn't announced. Minor; consider `role="status"` or an `aria-live` note. Calm-by-design otherwise solid (stable two-pane, no surprise swap).
- **Reaction picker is `role="toolbar"` but has no roving-tabindex/arrow-key nav** · 4.1.2 · `src/messaging/ConversationScreen.jsx:71` (ReactionPicker). Each emoji is a normal button (44px, labelled — good) and Escape works, but `role="toolbar"` implies arrow-key navigation that isn't implemented; SR users will hear "toolbar" and expect it. Either implement arrow nav or drop to a plain labelled group.

⚪ Minor
- **Composer textarea has no visible label** · only `aria-label` · `src/messaging/ConversationScreen.jsx:1548`. Functional for SR; a persistent visible label/placeholder is present ("Write a message…") so acceptable. No action required — noted for completeness.
- **`zoom: 1.15` for "Larger text"** · `src/App.jsx:302`. `zoom` is non-standard (works in Chromium/WebKit, ignored in Firefox). The footer note points users to browser zoom as backup, which mitigates. Consider `transform: scale` or root `font-size` if Firefox parity matters; low priority.
- Hardcoded backdrop rgba (`rgba(36,51,45,…)`) in modals is intentional and fine (overlays, not text surfaces).

**PASSED / held up well:**
- Skip link (`index.html:123` + `App.jsx:52`) — pure-CSS `:focus` reveal, jumps to `<main id="main-content" tabIndex=-1>`. Solid.
- Landmarks + nav: single `<header>`, `<main aria-label>`, `<nav aria-label="Primary">` on BOTH top (desktop/tablet) and fixed bottom (mobile) bars; `aria-current="page"` on active tabs; identical 4-destination IA for all users. Stable, predictable — good autism design.
- `document.title` + polite SR announce on every tab change (`App.jsx:481`, `:710`). S4 holds.
- Touch targets: ≥44px on nav tabs, message ⋯/＋/send (send 48px), reaction pills, chips, switches — consistently met.
- Focus management in flows: Onboarding focuses first invalid field on failed Continue (`:665`); AuthScreen focuses the error (`:38`); every modal/dialog/sheet (Delete, Unmatch, MatchMoment, ReportModal) focuses its heading on open, traps Tab, and restores focus on close; ConversationScreen restores focus to composer after delete. Strong.
- Forms: labels tied via `htmlFor`/`id`, `aria-required`, `aria-invalid`, `aria-describedby` for hints + live counters, `role="alert"` on inline errors. Onboarding/Auth/Report all correct.
- Reduced motion: dual-gated (OS `prefers-reduced-motion` + in-app toggle), global `transition-duration:0.001ms` sheet, plus per-component static fallbacks (MatchMoment/UnmatchSheet/AnimatedSpectrumMark render end-state). No autoplay/looping motion except the opt-in landing idle breath. Calm-by-design intent met.
- Decorative SVGs (icons, SpectrumMark, illustrations, avatars, glyphs) consistently `aria-hidden`/`focusable=false` with meaning carried by adjacent labels. Images have alt (`Shared photo`, `Attached photo preview`).
- Messaging log is `role="log" aria-live="polite"`, labelled, with a keyboard scroll hint. Reaction pills carry full meaning in `aria-label` (colour never sole signal). Deleted messages announce as tombstones.
- Dim-theme TEXT contrast (where tokens are used as intended) is excellent: textSoft 8.63:1, textMuted 5.35:1, accentStrong-as-text 6.76:1. The regression is strictly fill+white-text controls, not body text.

**Regressions vs the "last deploy fixed" list:** the messaging layer's accentFill/dangerFill adoption is correct, but the SAME pattern was NOT propagated to Button.jsx, LandingScreen, SafetyScreen, Onboarding, SuggestionScreen, or UnmatchSheet — so the dim-theme contrast fix is only half-landed. Hardcoded `#4E5F58`/`#FFF5F5`/`#FDF2F2`/`#B94040` literals flagged in the prior drift-island pass still remain in SuggestionScreen + ConversationScreen.

**Top items for the PM to prioritize:**
1. Fix dim-theme button contrast app-wide (🔴) — start with `Button.jsx` primary variant + `SuggestionScreen` "I'm interested" + `UnmatchSheet` confirm. Use `accentFill`/`dangerFill` (and add a `positiveFill`). This is the biggest real-user impact and a half-finished prior fix.
2. Replace remaining hardcoded `#4E5F58`/`#FFF5F5`/`#FDF2F2` literals with tokens (🟠) — Suggestion loading/error + Conversation alert panels.
3. `aria-current="page"` on the selected conversation row + VerifiedBadge light-theme text contrast (🟡).
4. Owed follow-up: re-run a live keyboard-tab + dim-theme visual pass on the deployed site once Chrome/Preview access is granted (this run was code-only). Focus-order and `:focus` ring visibility should be confirmed on real focus, not computed styles.

~A11y Director

## QA Front-End Analyst

### 2026-06-30 — QA pass (live functional/behavioral sweep on deployed site as admin: Discover, Matches, Messages 2-pane, Profile, Settings/Safety, Admin + code root-cause confirmation)

Method: exercised real flows on https://spectrum-dating-eta.vercel.app logged in as the admin account (`ttitleman`), watching console + network, and confirmed root causes against `src`. Accessibility/contrast is owned by the A11y Director and excluded here. Could NOT exercise true mobile/tablet widths — `resize_window` did not shrink the rendered viewport below 1920px (stayed desktop), and the logged-out Landing/Auth/Onboarding flow was not exercised (didn't want to destroy the admin session); both are owed follow-ups. No JS console errors or failed requests were observed on any screen visited.

🔴 Blocker
- *(none found in the flows exercised)* — core Discover, Matches, Messages send/react/delete, Settings, Safety, and Admin all functioned without errors or broken states.

🟠 High
- **Deployed code contradicts the "Moderation moved to Profile" status entry — Moderation is still a top-nav + bottom-nav tab, and ProfileScreen's `ModerationButton` is dead code.** · Repro: log in as admin → a 5th "Moderation" tab shows in the primary nav (desktop top bar + mobile bottom bar); Profile has NO "⚙ Moderation" button. · Expected (per STATUS 2026-06-29 "Responsive overhaul"): every user sees an identical 4-item nav and admins reach Moderation via a button inside Profile. · Actual: the OLDER design is live — `src/App.jsx:804-810` (top NavTab) and `:953-960` (BottomNavTab) still render Moderation for admins, while `src/ProfileScreen.jsx:2345-2346` is just a comment where the button used to render. `ModerationButton` (`ProfileScreen.jsx:2369-2395`) and the `isAdmin`/`onOpenModeration` props (`ProfileScreen.jsx:901`, passed from `App.jsx:894-895`) are now unused/dead. · Suspected cause: that status entry was written against a branch that was never the one deployed, OR the change was reverted. Functionally admins still reach Moderation fine (via nav), so user impact is low — but it's a real code/doc drift + dead code that will confuse the next dev. Decide which design is canonical and make code + STATUS agree.

🟡 Medium
- **Two competing scroll containers on desktop (`<main overflow:auto>` nested inside a `100vh` column-flex container that also overflows).** · Repro: on a tall screen (e.g. Profile), `<main>` grows to its full content height (measured 5562px) because it's `flex:1` in a column flex parent, so its own `overflow:auto` never triggers; the actual scroll happens on `document`/`body` (5727px). · Expected: a single, predictable scroll container. · Actual: `getComputedStyle(main).overflowY==="auto"` but `main.scrollHeight===main.clientHeight` (can't scroll); `window.scrollTo` and the visible page scrollbar confirm the *document* is what scrolls. Programmatic `window.scrollTo(0,2000)` worked; synthetic wheel events dispatched over `<main>` did not move content in-harness. · Location: `src/App.jsx:818-854` (`<main>` style) inside the `:697` `minHeight:100vh` flex column. · Suspected cause: the desktop "panel" refactor kept `overflow:auto` on `<main>` while the flex column lets `<main>` expand instead of clip. Real mouse wheels should still scroll via scroll-chaining (overscroll-behavior is `auto`), so this is likely benign for end users — but the dual-container setup is fragile (e.g. `overflow:hidden` is correctly applied only for the messages tab; every other tall screen relies on document scroll). Low confidence this bites real users; flagging for a deliberate decision (make `<main>` the single scroller with a real max-height, or remove its `overflow:auto`). Worth a quick real-mouse confirmation.

⚪ Low
- **`?skip=1` query param appears in the URL with no observable effect; the SPA has no client-side routing, so browser Back/Forward reloads the whole app to Discover rather than moving between in-app tabs.** · Repro: navigate to Messages/Profile, press browser Back → full reload, lands on Discover (`/?skip=1`), in-app tab state lost. · Expected/Actual: acceptable for a no-router SPA, but means deep-links/refresh always start at Discover and the deck rebuilds. Not a defect; noted so it isn't mistaken for one. Consider whether refresh-to-Discover is the intended resume behavior.

**PASSED / verified (coverage):**
- **Discover**: candidate card renders (avatar/photo, age, Verified badge, tagline, bio, About-talking, interests, why-reasons); "I'm interested" → calm "Saved…" confirmation + reassurance copy; "Skip" → "You won't see X again" confirmation; **Undo last skip restores the person to the FRONT of the deck** (`POST /matching/undo-skip` 200) — the undo-preserves-place fix holds; "Done for now" + "See suggestions again"; deck advances cleanly person-by-person; candidates already swiped don't reappear (server-side filtering), un-swiped ones do.
- **Messages (desktop 2-pane)**: list + thread render side-by-side with "Select a conversation" placeholder; row click opens correct thread; **send works** (`POST …/messages` 201, optimistic bubble); **reactions work** (picker → ♥ pill, `POST …/reactions` 200, youReacted fill) — confirms the send-response `id` alias + `userReacted` reaction fix; **delete works** (⋯ → confirm dialog → "Message deleted" tombstone); back button correctly hidden in 2-pane (`hideBack`).
- **Matches**: list renders with Verified badges; "Open chat" deep-links into the correct conversation in Messages — the `conversationId` alias / desktop-row fix holds.
- **Messaging field-mapping verified against live API**: server returns conversations with `{id, matchId, otherUser, lastMessageGroup, hasUnread}` (no `conversationId/unread/started/lastMessageLabel`); `api.js getConversations()` correctly derives all aliases; unread-count reads `c.hasUnread` correctly. (Note: admin currently has `hasUnread:false` on all 3 conversations, so no unread badge shows — this is data state, not a bug; the brief's "populated unread" expectation isn't met right now.)
- **Admin/Moderation**: stats row (36 members / 0 suspended / 8 matches / 30 messages / 0 open reports), status-filter tabs, "All" shows report cards with reporter→reported, email, reason, date, resolve dropdown + note + suspend — field normalization (names/emails) resolves correctly. (Did not actually resolve/suspend to avoid mutating data.)
- **Settings**: Warm-dim theme toggle applies live and reverts cleanly (functional; contrast is A11y Director's call). **Safety**: scripts render, Copy button → "Copied" confirmation (clipboard works).
- **Profile**: loads with photo gallery (Main badge + Add-photo tile), all field sections, Pause toggle, Identity-verification "Coming soon", Save changes, Sign out, Danger zone / Delete account. (Did not save/delete.)
- No console errors, no failed/4xx-5xx requests, no broken images observed across all screens visited.

**Top items for the PM to prioritize:**
1. 🟠 Resolve the Moderation nav vs. Profile-button discrepancy: pick the canonical design and make `App.jsx`/`ProfileScreen.jsx` + STATUS agree; remove `ModerationButton` + unused `isAdmin`/`onOpenModeration` props if nav-tab is canonical.
2. 🟡 Decide on the desktop scroll architecture (single scroller vs. document scroll); confirm real-mouse-wheel scroll on a tall screen — low confidence it affects users but the nested `overflow:auto` is fragile.
3. Owed follow-ups (could not exercise this run): true mobile/tablet-width pass (bottom nav, list→thread stack-swap, message-moment), and the logged-out Landing → Auth(register/login) → Onboarding (incl. DOB-18 gate) flow.

~QA Analyst

### 2026-06-30 — Launch-readiness re-verification (LIVE runtime audit on deployed site, both themes; prior 🔴/🟠 contrast fixes confirmed; landing-page landmark gap found)

Method: this run I had **live runtime access** (Chrome MCP) — exercised the deployed site https://spectrum-dating-eta.vercel.app logged in as the sample account (`mira.k.1`), drove Discover/Matches/Messages/Settings/Safety in BOTH themes, toggled Warm dim + High contrast + Larger text through the real UI, and computed WCAG contrast ratios numerically on the **actually-rendered** controls (not just source). Restored the sample account to clean defaults (light, all toggles off) before finishing. Mobile-width still could NOT be exercised (see owed follow-up). NOTE: the **deployed build is AHEAD of the local `src/`** — the dim-theme button fix is fully landed in production but only partially reflected in local source; my prior-run source-only findings on Landing/Safety/Onboarding/Auth buttons were therefore stale. Verified against the live deployment below.

**Headline: the dim-theme contrast regression I flagged last run is FIXED in production.** Every primary/destructive/filled control I could reach now passes AA in Warm dim. Re-verified live (dim theme): "I'm interested" + shared-interest pills = **6.27:1** (accentFill #356962); Safety "Share plan" + landing "Create your profile" = **6.41:1** (#3e6660); Button.jsx primary = **6.27:1**; UnmatchSheet confirm now uses dangerFill (#9E3B3B). The half-landed state I reported is resolved on the deployed site. Light theme unaffected. The biggest real-user risk from the last audit is cleared.

**Recent fixes — all CONFIRMED holding (live runtime):**
- ✅ **Nav `aria-current="page"`** — verified live on the active tab on Discover/Matches/Messages; `<nav aria-label="Primary">`, identical 4-item IA for the non-admin account.
- ✅ **accentFill / dangerFill / positiveText contrast tokens (both themes)** — defined in `index.html` for `:root` (light) and `:root[data-theme="dim"]`; runtime-measured passing. VerifiedBadge now uses `positiveText` (#4C7A47 light = **5.02:1**, was 3.59:1 — the prior 🟡 light-theme fail is fixed).
- ✅ **Skip link** — live: "Skip to content" → `#main-content`, present in the authed app.
- ✅ **`document.title` + polite announce** — verified updating per tab ("Discover/Matches/Messages/Safety Center/Settings · Spectrum") with a `aria-live="polite"` region echoing the screen name. S4 holds.
- ✅ **Form aria-invalid / focus** — labelled composer (`aria-label="Message Eli Brenner"`), `role="log" aria-label="Conversation with…"`, Send button 48px / `aria-label="Send"`.
- ✅ **AdminScreen aria-pressed** — not re-exercised this run (sample account is non-admin); confirmed present in source last run, no regression expected.
- ✅ **A11y toggles apply live** — High contrast → `filter:contrast(1.15)`, Larger text → `zoom:1.15`, both engaged at runtime and persisted to `spectrum_a11y`; theme radiogroup `aria-checked` correct; all four toggles are `role="switch"` with `aria-labelledby` + descriptions.
- ✅ **Dim theme is clean across the core app** — automated low-contrast sweep on Matches, Messages list, and the open conversation returned **zero** text/control failures. Tombstone "Message deleted" in dim = **4.71:1** (passes). Conversation danger-on-surfaceAlt panels = **4.89:1** (passes — the prior #FFF5F5/#FDF2F2 literals are now themed `t.surfaceAlt`).

**Prioritized findings (what could still embarrass us at launch):**

🔴 Blocker
- *(none)* — no AA-failing control or broken a11y affordance found on any surface I could reach in either theme.

🟠 Serious
- **Logged-out Landing page has NO `<main>` landmark and NO skip link** · 2.4.1 Bypass Blocks / 1.3.1 · live https://spectrum-dating-eta.vercel.app (logged out) — DOM is `header → section ×5 → footer`, no `<main>`, and the skip link only exists inside the authed `App.jsx` shell. This is the **first page every new visitor and screen-reader user hits**, and it's the one route a keyboard/SR user can't "skip to content" on. Fix: wrap the LandingScreen body in `<main id="main-content">` (and ideally render the skip link on the landing route too). Low effort, high visibility — it's the front door.
- **`document.title` says "Discover · Spectrum" while the LANDING page is showing** (logged out) · 2.4.2 Page Titled · live — the title doesn't describe the landing page; it inherits the app's default. A logged-out visitor's browser tab / bookmark / SR page-title announce all say "Discover", which is wrong and confusing. Fix: set the landing title (e.g. "Spectrum — dating at your own pace") when unauthenticated.
- **Local `src/` is behind the deployed build** · process/launch-risk · `src/LandingScreen.jsx:59`, `src/SafetyScreen.jsx:73`, `src/OnboardingScreen.jsx:850`, `src/AuthScreen.jsx:246` still use `t.accentStrong` as a white-text fill (would fail dim at 2.10:1) and `AuthScreen.jsx:246` / `SuggestionScreen.jsx:402` still hold hardcoded `#4E5F58`/`#B94040` literals — but the LIVE site renders the fixed (#3e6660) versions. The deploy is correct; the repo is stale. Before launch, reconcile so the source that ships matches what's deployed (otherwise the next build from local source REINTRODUCES the dim-contrast blocker). Confirm which branch/commit is the source of truth for the Vercel deploy.

🟡 Moderate
- **Desktop 2-pane empty placeholder ("Select a conversation to start reading.") is plain muted text, not announced** · 4.1.3 Status Messages · `src/messaging/MessagingApp.jsx` — unchanged from last run; when a row is activated focus moves into the thread (good), but the idle placeholder has no `role="status"`. Minor; consider a polite note.
- **Reaction picker `role="toolbar"` without arrow-key roving tabindex** · 4.1.2 · `src/messaging/ConversationScreen.jsx` (ReactionPicker) — unchanged; SR users hear "toolbar" and expect arrow nav that isn't implemented. Either implement it or drop to a labelled group. (Could not re-trigger the picker live this run, but source is unchanged.)

⚪ Minor
- **`zoom:1.15` for "Larger text"** · `src/App.jsx` — confirmed working live in Chromium, but `zoom` is non-standard and ignored in Firefox; the footer note pointing to browser zoom mitigates. Low priority — consider `transform: scale` / root font-size if Firefox parity matters.
- **Could not verify mobile (<600) at runtime** — `resize_window` resized the OS window but the page viewport stayed `innerWidth: 1920` and `matchMedia('(max-width:599px)')` never flipped, so the fixed bottom-nav, the mobile list→thread stack-swap, and MatchMoment on small screens are STILL unverified at runtime (same harness limit QA hit). Source review last run showed the bottom-nav markup is correct (role=tablist/tab, ≥44px, aria-selected, safe-area inset); a real device/emulator pass is still owed.

**PASSED / verified holding (live, both themes):** dim-theme primary/positive/danger fills all ≥4.7:1 · nav aria-current · skip link + main landmark (authed) · document.title + polite announce per tab · VerifiedBadge light-theme text now 5.02:1 · message log/composer/send labelling + 48px target · a11y toggles apply + persist · theme radiogroup + switch roles · Settings/Safety/Matches/Messages render calm and readable in Warm dim with zero contrast failures · focus rings visibly engage on real keyboard focus (← Back, nav tabs, toggles).

**What's needed before launch:**
1. 🟠 **Add `<main id="main-content">` + a skip link to the logged-out Landing page** and **fix its `document.title`** — it's the front door, it's the highest-visibility gap, and both are quick fixes. (2.4.1 / 2.4.2 / 1.3.1)
2. 🟠 **Reconcile local `src/` with the deployed build** so a future build from source can't reintroduce the dim-theme white-on-#8FBCB2 button regression (Landing/Safety/Onboarding/Auth bespoke buttons + the `#4E5F58`/`#B94040` literals). Confirm the canonical deploy source. This is a process/launch risk, not a live defect.
3. 🟡 Tidy the two carried-over messaging items (empty-pane `role="status"`, reaction-picker toolbar semantics) — non-blocking but cheap.
4. **Owed before sign-off:** a real mobile/tablet-width pass (bottom nav, stack-swap, match-moment) and the full logged-out Landing → Auth(register) → Onboarding DOB-18 gate flow — neither could be exercised in this harness (no sub-600 viewport, and I avoided destroying the session). Recommend an emulator/device run.

Net: **the core app is in good launch shape for accessibility** — the dim-theme contrast blocker from my last audit is resolved on the deployed site and all the recent fixes hold at runtime. The only items I'd hold launch on are the two 🟠 front-door fixes (landing landmark + title) and confirming the deploy source so the fix can't regress. Everything else is polish.

~A11y Director

## Design & Brand Director

### 2026-06-30 — Pre-launch brand & visual-polish review (landing persuasiveness, first-impression, type/spacing/color cohesion, animated logo + match moment, empty states, photography treatment, 2026 distinctiveness; both themes)

Method: source review of the brand layer — `LandingScreen.jsx`, `SuggestionScreen.jsx` (candidate card), `Avatar.jsx`, `MatchMoment.jsx`, `AnimatedSpectrumMark.jsx`, `illustrations.jsx`, `tokens.js`, `index.html` (theme vars, fonts, OG). NOTE: live-site capture was unavailable this run — Claude Preview AND Claude-in-Chrome tooling were both permission-denied (same blocker the A11y Director and QA Analyst hit), so this is a code-level read of the visual system, not runtime screenshots. A real device-width + dim-theme visual pass on the deployed site is still owed (see "Before launch"). Contrast/AA is owned by the A11y Director; I flag brand/market-appeal consequences, not duplicate the ratio audit.

**Headline: the *brand system* is genuinely distinctive and well-considered — calm, literal, "removed things" manifesto, serif/Atkinson pairing, the spectrum-tile mark, the quiet match moment. It does NOT read generic. The single biggest gap between "tasteful" and "converts + feels like a real 2026 dating product" is PHOTOGRAPHY: a dating app's core asset is faces, and right now real uploaded photos are shrunk to an 88px circle on the one screen that matters most (Discover). That, plus a sparse landing hero with no human warmth and no visual proof, is what most caps perceived quality and conversion.**

🔴 Critical (most raises perceived quality / conversion)
- **Candidate photos are rendered as a tiny 88px circle, never as a hero image** · Discover card · `SuggestionScreen.jsx:707-712` routes `photoUrl` through `<Avatar size={88}/>`, so even a candidate WITH a real uploaded photo appears as a small circular thumbnail beside their name — identical treatment to the no-photo gradient monogram. For a dating product this is the make-or-break visual; users decide on faces. The `ProfileScreen` gallery already does the right thing (square `aspectRatio:1/1`, `objectFit:cover`, `:294`/`:435`), so the app *has* proper photo treatment — it's just absent from the card. Fix: when `person.photoUrl` exists, render a large top-of-card image (full card width, ~4:3 or 1:1, rounded top corners, `object-fit:cover`) with the name/age/verified lockup below or overlaid on a soft gradient scrim; keep the 88px gradient avatar only as the no-photo fallback. This is the highest-leverage single change for first-impression and conversion.
- **Landing hero has zero human/product imagery — it's all type + abstract tiles** · `LandingScreen.jsx` hero (`:181-251`) · The page is well-written but a prospective user never SEES the product or a person before being asked to "Create your profile." In 2026 a dating landing page that shows no faces, no UI screenshot, and no proof reads as pre-launch/unfinished and depresses sign-up. Fix: add ONE calm, on-brand visual above or beside the CTA — a soft device mock of the calm Discover card, or a warm (non-stocky) lifestyle image with the brand color treatment — so the manifesto is backed by something tangible. (Keep it single + quiet; this is not a plea for a busy hero.)

🟠 Serious
- **Dim theme regresses the brand's filled controls to a washed-out look (and fails AA)** · app-wide · The A11y Director documented the contrast failure; the BRAND consequence is that in Warm dim the primary CTAs ("Create your profile", "I'm interested", "Say hello") render as pale mint fills with white text — they look disabled/unfinished, not premium. Half-landed `accentFill`/`positiveFill` fix. For a sensory-sensitive cohort, dim is a *flagship* theme, not an afterthought — it should look as polished as light. Tie the fix to brand QA, not just a11y.
- **The signature match moment is under-exposed — most users will never see it, and the landing only describes it** · `MatchMoment.jsx` is a real differentiator (calm, opacity-led, the spectrum "thread" drawing between two avatars) but it depends on photos to feel personal, and with the photo gap above it'll usually show two gradient monograms meeting — which undercuts the "two people connecting" emotion. Landing `:468-484` *tells* users about it but can't *show* it. Fix: (a) ensure the moment uses real avatars once the photo fix lands; (b) consider a tiny static preview of the moment on the landing as the "proof" visual above.
- **Two different "S" brand marks ship at once** · `public/icon.svg` favicon is a row of **5** tiles (per STATUS 2026-06-29 phase-1) while the in-app `SpectrumMark`/`AnimatedSpectrumMark` is **6** tiles. Minor but it's a logo-consistency tell; pick one tile count for the mark everywhere (favicon, OG, header, hero) so the brand reads as one system. Confirm `og.png` uses the same count.
- **Landing footer / closing copy repeats "Free to join. Leave whenever you like." verbatim twice** · `LandingScreen.jsx:248-250` (hero) and `:509-511` (closing CTA) · Repetition this close together reads as thin/placeholder. Vary the second instance (e.g. lead with a trust line) so the page doesn't feel templated.

🟡 Moderate
- **No social-proof or trust layer on the landing** · `LandingScreen.jsx` · No testimonial, no "built with [partner/community org]", no count, no press/endorsement, no privacy seal beyond a text line. The "Built with the community / Nothing about us without us" value is the brand's strongest trust asset but it's a small body paragraph. For launch credibility, elevate ONE concrete proof point (a named community collaborator, an advisory note, or a real quote) — autistic adults are (rightly) wary of being marketed *at*, and a generic claim without a name can read as appropriation. High brand-trust payoff.
- **Empty-state illustration metaphors are a touch obscure** · `illustrations.jsx` · `AllCaughtUp` is "a sun on a horizon" and `EmptyMatches` is "two cups + steam" — both pleasant, but without the caption the meaning isn't obvious (the sun reads as sunrise/sunset, not "caught up"). They're on-brand and calm (good), just lean hard on the adjacent copy. Low urgency; revisit only if doing an illustration polish pass.
- **Candidate tagline is serif-italic AND the bio is body — two serif treatments stacked close** · `SuggestionScreen.jsx:727-736` (italic serif tagline) sits right under a 30px serif name and above body. Three serif weights/styles in one lockup is slightly busy for a "calm" brand. Consider the tagline in `t.sans` (softer, less editorial) to let the serif name be the single serif moment in the lockup.

⚪ Minor / polish
- **OG description and landing subhead tell different stories** · `index.html:12` OG = "Match, then message whenever you're ready" vs. landing hero subhead = "No typing dots. No 'online now.' No rush." Both good; align the social-share hook to the manifesto voice (the "removed things" framing is the stronger, more distinctive hook — use it in OG too).
- **Hero headline vs. brand line mismatch** · brief's brand line is "Meet people at your own pace" and the hero uses exactly that (`:223`) — good — but the `<title>`/OG use "Dating at your own pace." Pick one canonical tagline and use it in title, OG, and hero for a single memorable line.
- **Idle "breathing" logo is on the hero only** · `AnimatedSpectrumMark idle` on landing `:197` — correct restraint; just confirm on the live site it's truly subtle at `brightness(1.06)` and not a distracting shimmer on the sensory-sensitive audience. (Owed: live check.)
- The assemble-on-mount logo, the spectrum progress indicator in onboarding, and the match-moment thread are a coherent, ownable motion language — this is a real strength; keep it consistent and don't add a second motion vocabulary.

**What's needed before launch (in priority order — what most raises perceived quality + conversion):**
1. **Fix Discover photo treatment (🔴).** Render real candidate photos as a large hero image on the card, gradient avatar only as fallback. This is the #1 conversion + "looks like a real dating app" lever and the gallery code to copy already exists in `ProfileScreen`.
2. **Give the landing one real visual (🔴) + one named trust point (🟡).** A calm device-mock/Discover-card preview (doubles as the match-moment "proof") and one concrete "built with [named community collaborator]" line. Backs the words with something tangible — the difference between "nice copy" and "credible 2026 product."
3. **Finish the dim-theme fill fix so flagship controls look premium in BOTH themes (🟠).** Coordinate with the A11y Director's `accentFill`/`positiveFill`/`dangerFill` items — this is brand polish AND accessibility in one pass; dim is a marquee feature for this audience.
4. **Unify the brand mark (one tile count) across favicon / OG / header / hero, and unify the tagline (🟠/⚪)** so the identity reads as one deliberate system.
5. **Owed: a live device-width + Warm-dim visual pass on the deployed site** (logged-out Landing → Auth → Onboarding, and the in-app Discover card + match moment) once Preview/Chrome tooling is permitted — to confirm the above on real renders, including the idle-logo subtlety and focus-ring visibility on real focus.

Bottom line for the PM: the brand foundation is strong and distinctive — type, color, voice, motion, and the calm-by-design ethos are launch-worthy and NOT generic. The gap is that it doesn't yet *show* what it promises: faces on the card, a tangible visual + named trust on the landing, and a dim theme as polished as light. Land items 1-3 and perceived quality jumps from "tasteful prototype" to "credible product."

~Brand Director

## Backend & Security Reviewer

### 2026-06-30 — Pre-launch security & backend readiness review (full source audit of Spectrum-Dating-Server/src + read-only live probes)

Method: full read of every file under `Spectrum-Dating-Server/src` (index, middleware, all 12 routers, socket, storage/R2, push, email, maintenance, matching, db/migrations) plus read-only live probes against the production API with the sample + admin logins. Confirmed live: admin authz returns 403 for a normal user (`/admin/reports`) and `/admin/me` → `isAdmin:false`; conversation IDOR returns 404 for a foreign id; malformed-JSON returns a generic "Bad Request" (no stack leak in prod); CORS `access-control-allow-origin` is locked to a single origin (`https://spectrum-dating-eta.vercel.app`). No data was mutated.

**Headline:** authorization, SQL parameterization, password hashing, and the per-row ownership checks are genuinely solid — this is a well-built backend. The launch-blocking risks are all about **secret/env hardening** (fallback secrets, no startup guard) and a **handful of auth-bypass / missing-rate-limit gaps**, not broken access control. The recently-fixed `join_conversation` handler is correct (try/catch + membership check + type guard).

#### 🔴 Blockers (fix before launch)

- **Hardcoded JWT fallback secret `'dev-secret-change-in-production'`** · risk: if `JWT_SECRET` is ever unset in prod, the app boots silently and signs/verifies tokens with a *public, source-controlled* secret → anyone can forge a token for any `userId` (full account takeover, admin impersonation). · `src/middleware/auth.js:4`, `src/socket/index.js:4`, `src/routes/export.js:7`. · fix: fail-fast at startup — `if (!process.env.JWT_SECRET) throw` in production; never ship a literal fallback. Define the secret in ONE module and import it (three copies drift).

- **`/export/conversations?token=` bypasses token revocation / suspension / deletion checks** · risk: the query-param JWT path calls `jwt.verify` only — it does NOT call `checkTokenVersion`, so a signed-out, suspended, or even deleted-then-reissued user's 30-day token still downloads their full message history (every conversation + message bodies, including archived). Confirmed live: `GET /export/conversations?token=<valid>` → 200 with attachment. · `src/routes/export.js:14-21`. · fix: route the query-param branch through the same `checkTokenVersion` logic as `requireAuth` (export the helper from auth.js and reuse it); reject suspended/revoked tokens.

- **Auth rate-limiter keys on spoofable client headers; weak brute-force posture** · risk: the limiter reads raw `x-real-ip`/`cf-connecting-ip`/`x-forwarded-for` headers directly (`auth.js:26-31`) rather than the proxy-validated `req.ip` — a client can forge/rotate these to reset its own rate-limit key freely, defeating the 20/15min cap. Combined with an 8-char-only password floor and no per-account lockout, credential-stuffing is viable. · `src/routes/auth.js:13-33,48-50`. · fix: have the limiter trust ONLY `req.ip` (with `trust proxy:1` already set), add a per-account/email lockout in addition to per-IP, and consider lowering `max` on `/login`.

#### 🟠 Serious

- **Mutation/abuse endpoints have NO rate limiting** · risk: only `/auth/*` (20/15min) and message-send (20/min) are limited. `/matching/swipe`, `/messaging/report`, `/messaging/block`, `/feedback`, `/photos/*` presign, `/push/subscribe`, `/profile` PUT are unthrottled → report-spam against a victim (fills the moderation queue), feedback-spam, swipe-flooding, and unbounded R2 presign calls (each an authenticated S3 round-trip). · `src/routes/matching.js:68`, `messaging.js:307,342`, `feedback.js:11`, `photos.js:57,167`, `push.js:16`. · fix: add a modest global per-user limiter (~100/min) and a tighter one on `/report` + `/feedback`.

- **R2 message-attachment "scan" is a no-op** · risk: `POST /photos/confirm/:id` just flips `upload_status` to `'scanned'` with zero actual scanning/review; the public URL is then served. Users can upload arbitrary image-typed bytes (CSAM/abuse risk on a dating platform) with no check — uploaded bytes are never validated, only the client-asserted MIME at presign time. · `src/routes/photos.js:205-221`. · fix: integrate real scanning or a human-review queue before serving, OR stop implying safety the status name suggests. Worth a deliberate safety call before launch.

- **Email verification is never enforced** · risk: `email_verified` defaults 0, is set by `/auth/verify`, and is returned to the client — but NO route requires it. A disposable/unverified email can onboard, match, and message. For a vulnerable-cohort dating app this weakens trust/safety and Sybil resistance. · no `requireVerified` middleware exists. · fix: decide policy; gate at least messaging behind verification, or document as a known launch gap.

- **Account deletion leaves R2 photos orphaned** · risk: `DELETE /account/me` cleans DB rows but never `deleteObject`s the user's `profile_photos.storage_key` / message attachments → personal photos remain publicly fetchable in R2 indefinitely (GDPR erasure + privacy). · `src/routes/account.js:13-24`. · fix: enumerate and best-effort-delete the user's storage keys inside the deletion transaction.

- **`reset-password` maintenance hook re-runs every boot from env vars** · risk: if `RESET_PASSWORD_EMAIL`/`RESET_PASSWORD_VALUE` are left set in Railway, the admin password is silently re-reset (and the action logged) on every deploy/restart; the plaintext new password lives in the env. · `src/maintenance/reset-password.js`. · fix: confirm both vars are UNSET in prod now; add a one-time marker so it can't re-run.

#### 🟡 Moderate

- **No central error handler** · `swipe`/`block` re-`throw` on non-UNIQUE DB failures and there is no terminal `app.use((err,...))`. Prod body is generic (verified — no stack leak) but error shape is inconsistent (HTML vs JSON) and 500s aren't logged uniformly. · `src/index.js`, `matching.js:102`, `messaging.js:331`. · fix: add a JSON error handler that logs server-side and returns `{error:'Something went wrong'}`.

- **No `helmet` / security headers; no explicit JSON body limit** · `src/index.js:52`. · fix: add `helmet()`, `express.json({ limit:'64kb' })`, and `app.disable('x-powered-by')` (the `x-powered-by: Express` header is exposed — verified live).

- **Presigned attachment uploads don't enforce declared MIME/size** · the presigned PUT accepts any bytes regardless of the `fileSizeBytes`/`mimeType` the client declared. · `src/routes/photos.js:167-200`. · fix: constrain `ContentType`/`ContentLength` on the presigned PUT or validate post-upload.

#### ⚪ Minor / good-to-know

- 30-day JWT lifetime with no refresh and only global (`token_version`) revocation — sign-out invalidates ALL sessions; no per-device revocation. Acceptable, noted.
- Migrations globally swallow "duplicate column" errors (`db.js:55`) — pragmatic but could mask a misordered migration.
- `/profile/prompt-catalog` and `/push/vapid-public-key` are intentionally unauthenticated (public scaffolding, no user data) — fine.
- **Confirmed-good:** every query is parameterized (the only dynamic SQL is `setClauses` built from a fixed allow-list `fieldMap`; candidate IN-clause uses `?` placeholders); bcrypt rounds=12; constant-time dummy-hash compare on login prevents user-enumeration timing; suspended users blocked at login AND on every authed request via `checkTokenVersion`; socket auth verifies JWT on connect and `join_conversation` re-checks membership (the recent crash-fix is correct); per-row ownership checks on messages/photos/matches/conversations (IDOR-safe, confirmed live); 18+ enforced in BOTH the profile-save validator AND the candidate filter (rejects rolled-over dates); `contextCard` correctly gated to post-match only; CORS locked to a single origin.

#### What's needed before launch

1. **Kill the fallback JWT secret + fail-fast startup guard** (🔴) — an unset `JWT_SECRET` in prod = full account/admin takeover; centralize the secret in one module.
2. **Fix `/export/conversations?token=` to run `checkTokenVersion`** (🔴) — currently a revocation/suspension/sign-out bypass leaking full message history; confirmed live.
3. **Harden the auth limiter to trust only `req.ip` + add per-account login lockout** (🔴).
4. **Rate-limit mutation/abuse endpoints** — especially `/report`, `/feedback`, `/swipe`, R2 presign (🟠).
5. **Make a deliberate call on the attachment "scan" no-op + the 18+/email-verification trust model** (🟠) — shipping fake "scanned" status is a safety liability for this cohort.
6. **Delete R2 objects on account deletion** (🟠) — GDPR/privacy.
7. **Confirm `RESET_PASSWORD_*` unset in prod; add `helmet`, JSON body limit, disable `x-powered-by`, central JSON error handler** (🟠/🟡).

Authorization, SQL safety, hashing, IDOR, and the 18+ gate are launch-ready. The blockers are env/secret hardening and two concrete auth-bypass paths — all fixable quickly and worth blocking on.

~Security Reviewer

## Product / Dating-UX Designer

### 2026-06-30 — Pre-launch product-completeness & UX review (dating-product + autism-friendly lens)

Method: full source review of every core flow (Landing → Auth → Onboarding → Discover → Matches → Messages → Profile → Safety → Settings → Moderation) in `C:\Users\Pen\Desktop\Spectrum-Dating\src` plus backend routes in `C:\Users\Pen\Desktop\Spectrum-Dating-Server\src\routes`. Live-site interaction was permission-denied this run, so behavioral confirmation leans on the QA Analyst's 2026-06-30 admin sweep; gaps below are read off code + product expectations, not runtime. Accessibility/contrast is the A11y Director's lane and isn't re-litigated here except where it is a *product* gap.

**Headline.** The calm-by-design promise is delivered unusually well in the parts that exist — no typing indicators / online-now / read receipts / streaks anywhere, "Why you're seeing X" reasons, undo-skip, profile pause, explicit-action-only swiping, no auto-advance, layered notification privacy (Off / silent / name-only), and a genuinely thoughtful Safety Center (scripts, date-plan share, check-in timer, privacy explainer). What is missing is mostly **table-stakes account/data plumbing plus a few autism-specific trust touches**, not the core loop. The launch-blocking theme: **account recovery and data-control surfaces a user reaches for on day one are either absent or built-but-unwired.**

🔴 Must-fix before launch
- **No password reset / account recovery anywhere.** · Why it matters: `AuthScreen.jsx` has no "Forgot password?" link and there is no reset/forgot route in the backend (no match in `routes/auth.js`). The moment a user mistypes or forgets their password they are permanently locked out with no self-serve path — for an audience that may lean on password managers and find support-loops highly stressful, this is a hard blocker. · Solution: email-based reset (request → emailed token → set new password) + a "Forgot password?" link on sign-in. · Effort: M.
- **Data export is built server-side but unreachable in the UI.** · Why it matters: `GET /export/conversations` exists and returns `spectrum-export.json`, and `getExportUrl()` exists at `api.js:345` — but nothing renders it (the helper is defined and never used). "Download my data" is a baseline trust + likely GDPR/CCPA expectation for an app holding intimate conversation data; shipping the endpoint while hiding the button is the worst of both worlds. (Note: the Security Reviewer separately flags that this same `?token=` export path bypasses revocation — fix both together.) · Solution: add a "Download my data" link in Profile near Sign out / Danger zone that hits `getExportUrl()`; consider widening the payload beyond conversations to profile + matches. · Effort: S to wire; M to broaden.
- **No visible blocked-users list / unblock.** · Why it matters: users can block from a candidate card or a chat, but no screen shows who they have blocked or lets them undo it (no `getBlocked`/`unblock` in `api.js`, no UI). Reports are reviewable ("Your reports" in Safety) but blocks are a one-way trapdoor. Accidental blocks happen, and irreversibility erodes the control this audience specifically needs. · Solution: a "Blocked people" list (Settings or Safety) with Unblock (needs a backend list + delete endpoint). · Effort: M.

🟠 Strongly recommended before launch
- **No email-change and no password-change for a logged-in user.** · Why it matters: no way to update the email a verification link is tied to, nor to rotate a password without the (also-missing) reset flow. Combined with the 🔴 above, a user who loses inbox access or wants to rotate credentials has no path but account deletion. · Solution: an Account section (Profile/Settings) with change-email (re-verify) + change-password. · Effort: M.
- **Discover can't distinguish "deck exhausted" from "no people in your area yet."** · Why it matters: the empty Discover state ("No new suggestions right now… try adding interests") shows the same copy whether a real deck was exhausted or the platform simply has no eligible candidates nearby — a near-certainty at launch with a small base. Telling an early user to "add more interests" when the real cause is an empty pool reads as the app blaming them. · Solution: have the backend signal pool-empty vs deck-exhausted; show honest copy ("We're still growing in your area — we will let you know as people join"). · Effort: S–M.
- **MatchMoment's "Say hello" does not land the user in the new thread.** · Why it matters: the hardest moment for many autistic users is the blank opener. `EmptyConversationState` + the `getStarters` API solve exactly this — but `onOpenChat` just calls `onOpenMessages()` (drops the user on the Messages tab; `matchId` is captured but unused per the in-code comment at `SuggestionScreen.jsx:890-895`). So the calm hand-off ends with the user hunting for the new conversation themselves, never seeing the starters. · Solution: deep-link "Say hello" straight into the new thread (create-conversation + open). · Effort: S–M (already noted as a TODO in the code).
- **No re-engagement signal when a deck refills or a new match arrives while away.** · Why it matters: the product rightly strips urgency cues, and empty states promise "we will let you know" — but push is opt-in and there is no email/in-app digest fallback. A calm async product still needs *some* quiet way to bring people back or they simply do not return. · Solution: ensure new-match/deck-refill events can drive the existing push; consider a low-frequency opt-in email digest. · Effort: M.
- **Report is always fused to block, and reason taxonomies differ by surface.** · Why it matters: both `BlockReportScreen` (chat) and the pre-match `ReportModal` always block when you report — sometimes a user wants to flag behavior to moderators while still deciding. Reasons also diverge (pre-match: inappropriate/spam; chat: harassment/spam/inappropriate/other), which complicates moderation triage and the user mental model. · Solution: offer report-without-block; unify the reason list across surfaces. · Effort: S–M.

🟡 Worth doing soon
- **Onboarding never collects the "moat" comms/sensory dimensions or prompts the rest of the app leans on.** · Why it matters: Discover cards show "How I communicate" chips, sensory prefs, and Hinge-style prompts, and Profile lets you set them — but onboarding only gathers name/DOB/tagline/bio/interests/comm-note/goal. New users start with exactly the autism-*specific* fields empty, so early decks look generic and the differentiator is invisible until someone digs into Profile. · Solution: one optional, clearly-skippable onboarding step for a couple of sensory/comms prefs or a single prompt. · Effort: M.
- **No "preview my profile as others see it."** · Why it matters: predictability is a core need; users cannot see the exact card a match sees, so they are guessing what they shared. · Solution: a "Preview my card" rendering the SuggestionScreen card layout from own data (component already exists). · Effort: S–M.
- **The active-conversation cap is only explained at the moment it is hit.** · Why it matters: `CAP_REACHED` copy ("Archive one to start a new chat") appears only on failure (`MatchesScreen.jsx:197`). A user budgeting limited social energy benefits from seeing the cap/usage *before* bumping into it. · Solution: show cap usage calmly in Messages. · Effort: S.
- **Verified badges render everywhere but verification is "Coming soon."** · Why it matters: badges appear on candidate cards, matches, and Profile, yet no one can actually get verified — a trust signal nobody can earn reads as inconsistent (or fakeable). · Solution: ship a basic verification path, or hide badges until it exists. · Effort: M to ship / S to hide.
- **Chat photo attachments are fully built but dark (`ATTACHMENTS_ENABLED` off).** · Why it matters: not a blocker, but the paperclip, preview, and scan states are all coded and hidden — make a deliberate in/out call so it is not half-discoverable. (Security Reviewer also flags the attachment "scan" is a no-op — resolve before enabling.) · Effort: S decision / M enable.

⚪ Minor / polish
- Safety check-in timer + date-plan are device-local only (clearly stated) — fine, but a returning user on a new device silently loses an in-flight timer; one line of copy would set expectations. · S.
- No block/report entry point from **Matches** rows or a match's profile *before* opening the chat — only pre-match (candidate card) or inside an open conversation. A user who matched then felt uneasy must open the chat to act. · S.
- The pre-match "Report {name}" flow auto-closes after 1.2s and reports+blocks irreversibly (ties to the no-unblock 🔴) — a mis-tap cannot be undone. · S.
- Relationship-goal is collected but I did not see it surfaced to the viewer as a why-reason or filter — confirm it influences discovery or drop the friction. · S to verify.

**What's needed before launch (short list):**
1. 🔴 Ship **password reset / account recovery** — no user should be one typo from a permanent lockout.
2. 🔴 **Wire the existing data-export** into Profile ("Download my data") — the endpoint is already built (and fix its revocation bypass alongside the Security Reviewer's item).
3. 🔴 Add a **blocked-people list with unblock** — make blocking reversible and visible.
4. 🟠 **Change email / change password** for logged-in users.
5. 🟠 Honest **cold-start empty-Discover copy** (pool-empty vs deck-exhausted) so early users are not told to "add interests" when the pool is just thin.
6. 🟠 **Deep-link the MatchMoment "Say hello"** into the new thread so the starter scaffolding actually catches the user.
7. 🟠 A **re-engagement signal** for new matches / deck refills beyond opt-in push.

Bottom line for the PM: the core calm-dating loop is genuinely strong and the autism-friendly promise is met where features exist. The launch gaps are the boring-but-essential account/data/recovery surfaces (recovery, export, unblock, credential changes) plus a few autism-specific trust touches (cold-start honesty, the opener hand-off, verification consistency). Land the three 🔴s and this is launchable.

~Product Designer

### 2026-06-30 — QA pass #2 (live-API + code root-cause sweep filling prior owed gaps: register→onboarding→18-gate, messaging field-shapes, reaction PERSISTENCE, photo/attachment 503 paths, swipe/undo, 409-open-existing, pause, contextCard leak)

Method: live Chrome-driving MCP was **permission-denied** this run (and computer-use grants browsers only read-tier), so no on-device click-through. I instead exercised the **live backend API directly** (curl against the Railway prod backend) as the sample user (`mira.k.1`), the admin (`ttitleman`), and a **freshly-registered throwaway account**, then root-caused every behavior against `src` (frontend) and `Spectrum-Dating-Server/src` (backend). This fills the prior QA pass's explicitly-owed gaps (logged-out register→onboarding, the 18+ gate) and surfaces one real defect that an in-session-only UI test cannot see. All sample-account mutations were reverted (test message deleted, reaction toggled off, skip undone, pause reverted).

🔴 Blocker
- *(none)* — auth, register, onboarding 18+ gate (client AND server), candidates, matches, send/delete, swipe/undo, 409-open-existing, pause, and admin authz all functioned on the live API.

🟠 High
- **Message reactions do not persist — they vanish when a conversation is reopened or reloaded.** · Repro (API-confirmed): `POST /reactions/messages/:id/reactions {emoji:"♥"}` → `200 {reactions:[{emoji,count,userReacted}],action:"added"}` and the row IS written to `message_reactions`; but immediately re-fetching `GET /messaging/conversations/:id` returns each message with keys only `[id, senderId, body, deleted, timeLabel]` — **no `reactions` field**. · Expected: a previously-added reaction shows as a filled pill after reopening the thread. · Actual: reactions render only optimistically (on click) and via the live `reaction_update` socket event for the *current* session; on reload/reopen they disappear even though they're stored server-side. · Location / root cause: **backend** `Spectrum-Dating-Server/src/routes/messaging.js:99-112` — the conversation-detail query selects only `id, sender_id, body, deleted, sent_at` and never attaches reactions. The helper `getReactionSummary(db,messageId,userId)` already exists (`routes/reactions.js:10-26`) but conversation-detail calls neither it nor the per-message GET. The **frontend is already correct and waiting**: `src/messaging/ConversationScreen.jsx:821-830` hydrates from `msg.reactions` on load — so the fix is purely server-side (attach `reactions: getReactionSummary(db, m.id, userId)` per message in the `.map`). · Why the prior pass missed it: in-session testing shows the optimistic/socket state, which masks the reload gap. Reactions are a shipped, advertised feature — fix or consciously accept before launch.

🟡 Medium
- **Prior 🟠 "Moderation discrepancy" is now PARTIALLY RESOLVED — but two stale comments remain.** · Update to QA pass #1: the dead `ModerationButton` and the unused `isAdmin`/`onOpenModeration` props have since been **removed from `ProfileScreen.jsx`** (grep returns zero matches) and `App.jsx` renders `<ProfileScreen/>` without them. The canonical design is now unambiguously "Moderation = admin-only 5th nav tab" (`App.jsx:804-810` top, `:951-957` bottom) and it works. **However**, two comments still falsely claim the abandoned design — `App.jsx:772` and `App.jsx:910` both say "Moderation lives in Profile" — and the 2026-06-29 "Responsive overhaul" entry above describes a Profile-button design that did not ship. Fix: correct those two comments + that STATUS entry. Pure doc/comment drift, no user impact; the dead-code half is fixed.
- **New user who matches before ever opening Profile gets a generic "you" avatar in the MatchMoment overlay.** · Root cause: `SuggestionScreen.jsx:12 getViewerIdentity()` builds the celebratory overlay's "you" lockup from `localStorage["spectrum_profile"]`, but that cache is written ONLY by `ProfileScreen.jsx` (on load `:1070` and save `:1319`). A freshly-registered user who finishes onboarding and goes straight to Discover has never mounted ProfileScreen → cache empty → the "same wavelength" moment shows a default gradient avatar with no name/photo for the viewer (the *other* person renders fine from the candidate). · Fix: seed `spectrum_profile` from the App-level `getProfile()` that already runs at auth (`App.jsx:569`), or have `getViewerIdentity` fall back to a fetch. Cosmetic, celebratory-moment-only.

⚪ Low
- **Registration → brief Discover flash before onboarding mounts.** · `App.jsx:679-695`: after register, `authed` flips true and the main app (Discover) renders because `onboarding` defaults false; only after the async `getProfile()` (`:567-581`) resolves does `setOnboarding(true)` swap in OnboardingScreen. Confirmed harmless: a profile-less new account's `GET /matching/candidates` returns `[]` (200, not an error), so the flash is at worst a sub-second "no suggestions" empty state. Could gate the app behind an "onboarding-known" flag if undesirable.
- **Message photo attachments won't persist even once R2 is configured (compounds the known R2 blocker).** · `ConversationScreen.jsx:1019-1038` + its own TODO (`:1020-1022`): `sendMessage` doesn't accept an `attachmentId`, so an attached photo is shown only optimistically via the R2 `publicUrl`; the persisted message row carries no photo and it's lost on reload. Currently moot — attachment upload itself 503s (R2 not configured) and that 503 is handled gracefully (`:1049-1050`). Flagging so it isn't assumed "done" the moment R2 lands; the backend message↔attachment link is still needed. (Product Designer separately flagged attachments are dark behind `ATTACHMENTS_ENABLED`.)

**PASSED / verified this run (live API + code root-cause):**
- **18+ gate, both sides:** client blocks <18 in onboarding (`OnboardingScreen.jsx:643-645`, with `max` attr + `ageFromDob` leap-day guard); server independently rejects — `PUT /profile/me {dateOfBirth:"2015-01-01"}` → `400 "You must be 18 or older…"`.
- **Auth robustness:** register `201`+token; duplicate email `409`; wrong password `401 "Invalid email or password."`; admin login `200 isAdmin:true`; password policy ≥8 chars on BOTH client and server (no hidden-rule mismatch). 401 anywhere → `auth:expired` clears token + routes to sign-in (`api.js:54-57`, `App.jsx:551-564`).
- **New-user onboarding handoff:** fresh account returns `onboardingComplete:false` + all-empty profile; `App.jsx:567-571` flips onboarding; `candidates/matches/conversations` all return empty arrays (200) pre-onboarding — no errors.
- **Messaging field-shapes (live):** `getConversations` server shape `{id,matchId,otherUser,lastMessageGroup,hasUnread}` correctly aliased in `api.js:165-173`; conversation-detail messages carry `id` (send-response `messageId→id` alias holds); **send** `201 {messageId,timeLabel}` + **delete** `200 {deleted:true}` verified live and reverted.
- **Swipe/undo deck-place:** `POST /matching/swipe {decision:"skip"}` → `200 {matched:false}`; `POST /matching/undo-skip` → `200 {ok:true,candidateId}` (returns the id so the deck restores it). Verified + reverted.
- **409 open-existing:** `MatchesScreen.jsx:189-194` reads `e.body.conversationId` on 409; server returns it (`messaging.js:157`). Holds.
- **Graceful 503s:** profile photo presign, photo-add, and message-attachment intent all `503 "…not configured."` with friendly copy (`ProfileScreen.jsx:1124-1130`, `ConversationScreen.jsx:1049-1050`) — consistent with the known R2 blocker, no crash.
- **contextCard pre-match leak:** `GET /matching/candidates` no longer includes `contextCard` (verified absent on all 10 candidates); post-match `match.otherUser.contextCard` still present (correct). The pre-match suppression shipped.
- **Pause / reaction-emoji set / reports:** `paused` round-trips; the emoji the frontend sends (`♥ 👍 😊 😄 🤔`, `ConversationScreen.jsx:41-47`) exactly matches the server allow-list (`reactions.js:8`); `my-reports` returns status data.

**Coverage I could NOT do this run (owed, same harness limit as other agents):**
- **No live UI click-through** — Chrome MCP permission-denied, browsers read-tier under computer-use. So no visual confirmation of rendering, focus order, the **mobile bottom-nav / list→thread stack-swap**, the desktop 2-pane, or the **MatchMoment overlay** on the real deployed site. Findings above are live-API behavior + source root-cause, not pixels. A subsequent run with Chrome/Preview access should still do the visual mobile/tablet + match-moment pass (owed across this and the prior QA entry).
- Did not exercise rate-limit (429) or the consent-gate (403) live, to avoid tripping protections / mutating state. (Security Reviewer separately notes most mutation endpoints lack rate limiting.)

**What's needed before launch (QA lens — not re-litigating other agents' items):**
1. **🟠 Make reactions persist** — attach `reactions` to each message in `GET /messaging/conversations/:id` (`messaging.js:99-112`); the frontend already consumes it. Only newly-found functional defect in a shipped feature; decide fix-or-accept.
2. **Known blockers re-confirmed (not re-litigated):** R2 photo storage `503` (profile photos + message attachments), and transactional email not configured (`emailVerificationEnabled:false` live — verification silently disabled; also means no password-reset email path, which the Product Designer flags as a 🔴).
3. **🟡 Doc/comment hygiene:** correct the two false "Moderation lives in Profile" comments in `App.jsx` (and the matching 2026-06-29 STATUS entry); optionally seed `spectrum_profile` at auth so the MatchMoment "you" avatar isn't generic for brand-new users.
4. **⚪ Polish:** gate the post-register Discover flash; plan the backend message↔attachment link so message photos persist once R2 lands.

~QA Analyst

## Backend & Security Reviewer

_Date: 2026-06-30_

POLL of what's **MISSING** for production (already-done items per brief excluded). Live header check confirms no security-header middleware: response leaks `x-powered-by: Express`, no HSTS / CSP / X-Frame-Options / X-Content-Type-Options.

**[Critical]**
1. **No `helmet` / security-response headers.** Confirmed live: missing HSTS, CSP, X-Content-Type-Options (nosniff), X-Frame-Options; `x-powered-by` exposed. → clickjacking, MIME-sniffing, downgrade, framework fingerprinting.
2. **No error-handling middleware + no error tracking (Sentry/equiv).** No central `app.use((err,req,res,next))`; uncaught throws return default Express handler and may leak stack traces; no `process.on('unhandledRejection'/'uncaughtException')`. → crashes go unobserved, internals leak.
3. **No moderation audit log.** suspend / unverify / report-resolve / verify mutate state with no append-only record of moderator, target, timestamp, reason. → no accountability, no abuse-of-admin trail, weak Trust & Safety/legal posture.

**[High]**
4. **No application monitoring / uptime alerting / structured request logging.** Only ad-hoc `console.log`; no `/health` polling alert, no metrics, no log aggregation. → outages and error spikes go unnoticed.
5. **No per-account brute-force lockout.** Rate-limit is per-IP only (20/15min); distributed/rotating-IP credential stuffing against a single account is unthrottled. No account-level counter or progressive lockout.
6. **No abuse / spam detection.** No message-flood throttle, duplicate-account / disposable-email / signup-velocity checks, or report-driven auto-flagging. → spam, scammers, ban-evasion via re-registration.
7. **No GDPR data-retention automation.** Expired `email_verifications` purged only opportunistically; no scheduled purge of expired tokens, resolved reports, blocked/stale records, or inactive-account aging. → unbounded retention of personal data.

**[Medium]**
8. **No explicit request body-size limits.** `express.json()` uses the implicit 100kb default with no per-route caps; bodies aren't deliberately bounded. → memory-pressure / abuse vector; should be set intentionally.
9. **No 2FA / TOTP option.** No second factor available even opt-in for a sensitive account type (location, photos, private chats).
10. **No password strength / breach check.** Only length >= 8; no zxcvbn/HIBP-style rejection of weak or known-breached passwords.
11. **No secrets-rotation story.** JWT signing secret is a single static env var; no key-id/rotation support, so rotating it logs everyone out and there's no documented procedure.

**[Low]**
12. **No CSRF consideration documented.** Token-in-header auth makes this low-risk, but it's unstated; worth an explicit note since CORS is currently single-origin only.
13. **No dependency / vulnerability scanning** (Dependabot / `npm audit` in CI) wired into the deploy pipeline.

~Security Reviewer

## Design & Brand Director

### 2026-06-30 — MISSING brand/marketing/conversion poll (launch-readiness gap audit)

Ranked list of items a launch-ready 2026 product would have but that are ABSENT from the landing page, public site, and brand surface. Verified against `src/LandingScreen.jsx`, `index.html`, `public/manifest.json`, and App routing (`App.jsx` has no public marketing routes beyond Landing).

1. **[Critical] No social proof / trust signal anywhere** — zero testimonials, member count, named community partner, or "as featured in." For an autistic-audience product, lived-experience endorsement is the #1 trust driver; the "built with the community" claim is currently unsubstantiated.
2. **[Critical] No Privacy Policy or Terms pages** — a dating app collecting PII has legal exposure and app-store rejection risk without them. Privacy is only described prose-style in the in-app SafetyScreen; no public, linkable policy and no footer legal links.
3. **[Critical] Landing hero has no human/product visual** — hero is logo + headline + buttons on flat gradient; no faces, screenshots, or product glimpse. Visitors can't see what they're joining, which depresses sign-up conversion.
4. **[High] No email / launch-list capture** — only CTA is "Create your profile." No "notify me / join the list" path for visitors not ready to register, so all non-converting traffic is lost with no re-engagement hook.
5. **[High] No App Store / Play Store presence or badges** — PWA-only; no store listings, no "available on iOS/Android," no phone-in-hand screenshots. In 2026 most daters expect a native app and store-badge legitimacy.
6. **[High] No FAQ / About / founder-story page** — niche, mission-driven product with no place to explain "why this exists," who's behind it, or answer "is this safe / is it really free." Mission-led brands live or die on the about narrative.
7. **[High] Favicon/mark inconsistency (5-tile favicon vs 6-tile in-app mark)** — brand mark must be identical everywhere; a mismatched favicon reads as unfinished and erodes the calm/considered brand promise.
8. **[Medium] No Contact / support page** — no email, no support route, no way to reach a human. Trust-sensitive audience expects an obvious "talk to us."
9. **[Medium] No referral / invite mechanism** — dating apps grow on word-of-mouth; nothing lets a happy member bring a friend, and "nothing about us without us" begs for an invite loop.
10. **[Medium] No cookie / consent notice** — fonts load from Google; any analytics would need consent. Absent banner is a GDPR/UK-GDPR gap and a missing trust cue.
11. **[Medium] Generic meta description ("Dating at your own pace.")** — `<title>` is plain "Spectrum Dating" and description is one line; no keyword-rich SEO copy, no structured data. OG tags exist (good) but discoverability is thin.
12. **[Low] No custom 404 / branded error page** — off-route visitors fall to a default; a calm branded 404 is low effort and on-brand polish.
13. **[Low] No press / media kit** — no logo downloads, brand colors, or boilerplate for journalists/partners covering an accessibility-forward launch.
14. **[Low] No onboarding delight / welcome moment beyond functional flow** — match-moment is built, but no first-run "welcome to the calm" beat to reward sign-up and set tone.
15. **[Low] No favicon `.ico` / Safari pinned-tab parity check** — SVG + PNG favicons present; verify legacy `.ico` and pinned-tab mask render the corrected mark once #7 is resolved.

~Brand Director

---

## Accessibility Director — 2026-06-30

POLL: what's **MISSING** for an autism-friendly, WCAG 2.2 AA, calm-by-design audience. Already-built items (dim/contrast/larger-text themes, reduce-motion, skip link, landmarks, aria-current, per-screen titles, SR announcements, focus mgmt, 44px targets, calm-by-design omissions) are NOT relisted. Ranked gaps:

1. **[Critical] No descriptive alt text for user photos.** `Avatar.jsx` hardcodes `alt=""` on every profile/uploaded image and message photos use a generic `alt="Shared photo"`. On a *dating* app, photos carry the core meaning — blind/low-vision users get nothing. Need an alt/description field at upload and a sensible fallback (e.g. person's name) instead of empty alt.

2. **[High] No reduced-/simplified-sensory profile or "calm content" controls beyond motion.** Calm mode only flattens background + kills motion. Missing: emoji/reaction muting, decorative-illustration toggle, and a single "low-stimulation" master switch — high-value for sensory-sensitive autistic users.

3. **[High] No plain-language / simplified-text mode.** No mechanism to surface shorter, literal, idiom-free copy (onboarding, errors, safety). Literal-language support is one of the highest-impact autism accommodations and is entirely absent.

4. **[High] No session-timeout / inactivity warning.** Auth silently 401s and dumps the user to sign-in with "session expired" (`App.jsx` auth:expired). WCAG 2.2 SC 2.2.1 expects a warning + a way to extend. Abrupt logout mid-message is disorienting and loses drafted text.

5. **[High] No accessibility statement / "how we support you" page.** No discoverable page documenting features, conformance, known limits, and a contact route. Expected for WCAG-committed products and reassuring for this audience; also a credibility signal.

6. **[Medium] No text-spacing controls (line-height / letter / word / paragraph).** Only a 15% zoom ("larger text"). WCAG 2.2 SC 1.4.12 expects content to tolerate user spacing; offering in-app spacing sliders directly aids dyslexic and many autistic readers.

7. **[Medium] No font choice / dyslexia-friendly option.** App is locked to Atkinson Hyperlegible (good default) + Newsreader serif for headings. No opt-in alternative (e.g. a dyslexia-tuned face) and no way to disable the serif display font, which some find harder to scan.

8. **[Medium] No content/sensitivity warnings or pre-message expectation-setting.** No content-warning affordance on profiles/messages and no upfront "what to expect" framing before opening a chat. Predictability reduces anxiety for autistic users; surfacing it explicitly is missing.

9. **[Medium] No keyboard-shortcut help / discoverability.** Useful in-thread keys exist (Esc closes menus, arrow-nav in menus, PageUp/Down hint in the log) but there's no `?`-style shortcuts panel or visible affordance, so they're undiscoverable.

10. **[Medium] Messages region lacks a persistent landmark + clear new-message SR semantics.** The thread uses `role="log" aria-live="polite"` (good) but there's no `<nav>`/landmark structure inside Messages and no heading-level map for SR users to jump between conversation header, log, and compose. Add explicit landmarks/headings within the messaging view.

11. **[Low] Reaction-picker lacks arrow-key navigation.** Known gap. The picker is `role="toolbar"` but only focuses the first button; left/right arrow roving (per WAI-ARIA toolbar) is absent. Message/header menus already do arrow-nav — bring the picker to parity.

12. **[Low] No prefers-* / OS-setting sync for contrast & text size.** Reduce-motion respects the OS query, but high-contrast and larger-text are in-app only; honoring `prefers-contrast` / `prefers-reduced-data` and OS text-zoom would reduce setup friction.

13. **[Low] No skip mechanism within long scroll views (Discover/Matches).** Single global skip link only. A "skip to compose" / "skip to filters" within dense screens would cut keyboard travel for motor- and attention-affected users.

~A11y Director

---

## Product / Dating-UX Designer

_Poll date: 2026-06-30 — what's MISSING that a dating product needs (not a bug audit). Tags: [Critical]/[High]/[Medium]/[Low]. Excludes already-built features and the known founder-blocked items (R2, email, legal pages, domain)._

### [Critical]
1. **No gender / pronouns / who-you're-seeking — anywhere.** There is no gender, orientation, or "looking to meet" concept in the frontend or backend (grep: zero matches; `candidates.js` matches purely on interests + lifestyle deal-breakers). A dating app cannot ship without letting people say who they are and who they want to see — right now everyone is shown everyone. This is the single biggest table-stakes gap. For an autistic audience, explicit, structured identity/seeking fields (vs. inferred) are also a better fit than most apps.
2. **No discovery preferences/filters.** Beyond 3 hard deal-breakers (local, non-smoker, wants-children) and a distance radius, the viewer can't set an age range, a "show me" gender filter, or any positive preference. `getCandidates` has no age-range or gender filter. Users expect to scope who appears in Discover; today it's interests-score only.
3. **Can't change email or password while logged in.** `account.js` exposes only `DELETE /me`; `auth.js` only resets password via an emailed token (and email sending is founder-blocked, so reset is effectively dead too). A logged-in user has no in-app "change password" or "change email." This is basic account hygiene every product has — and the absence is sharpened because the email-token path is unavailable.

### [High]
4. **No identity/photo verification flow.** `verified`/`VerifiedBadge` exist but are read-only — set by an admin, with no user-facing "verify me" path (selfie/photo challenge). For a safety-sensitive autistic audience, a self-serve verification step is a trust feature users will look for, not a nice-to-have.
5. **No profile-completeness guidance.** Onboarding requires only name + 1 interest + bio; the rich "moat" fields (comms style, sensory prefs, prompts, photos, lifestyle) are all optional with no completeness meter, no nudge, and no "your profile looks empty" prompt. Thin profiles hurt match quality and are a known retention lever. A calm, non-gamified completeness hint fits this audience well.
6. **No notifications / activity inbox.** Push opt-in exists, but in-app there's no list of "X liked you," "new match," "new message" events — only a live unread badge on the Messages tab. Users expect a place to see who's expressed interest and recent activity. (Note: no "who liked you" surface at all — likes are invisible until mutual.)
7. **No read receipts or typing indicator shown to the other person.** Read cursors exist server-side (`008_read_cursors.sql`, drives only the sender's own unread dot) but the recipient never sees "Read" or "typing…". For literal-communication users especially, knowing whether a message landed reduces anxiety; its absence is felt. (Make it optional/toggleable to respect those who'd find it pressuring.)
8. **No conversation list search or message search.** With multiple matches there's no way to find a person or a past message. Standard in every messenger; missing here.

### [Medium]
9. **Onboarding is shallow on the differentiators.** The 3-step onboarding collects basics + interests + a free-text comms note, but never walks the user through the sensory/communication-style structured fields that are this app's whole reason to exist — they're buried in the profile editor and easy to never fill in. Bringing 1–2 of them into onboarding would lift the feature that differentiates Spectrum.
10. **No re-match / "you unmatched" recovery, and no unmatch reason.** Unmatch exists but is one-way and final; no way to recover an accidental unmatch and no record/feedback. Also no block/unmatch distinction explained to the user.
11. **Photo sharing in chat is dark.** `ATTACHMENTS_ENABLED = false` and `sendMessage` can't link an attachment. Sending a photo to a match is an expected messaging capability (partly tied to the R2 blocker, but the message↔attachment backend link is missing independent of R2).
12. **No "first move" / icebreaker structure beyond starters.** Conversation starters exist, but there's no scheduled/low-pressure prompt exchange or structured first-message flow — a strong fit for users who find open-ended openers hard. Opportunity, not just a gap.
13. **No way to hide/skip specific profile fields from view, or control field-level visibility.** Profile is all-or-nothing (plus the pre-match contextCard suppression). Granular "who can see what" controls are increasingly expected, especially by a privacy-conscious audience.

### [Low]
14. **No dark/light auto (system) theme option** — theme is manual light/dim only; no "match system."
15. **No match/conversation sorting or filtering** (recent, unread, new) in the Matches/Messages lists.
16. **No "preview my profile as others see it"** — users can't see their own card the way a candidate does.
17. **No saved/favorite or note-to-self on a match** — no lightweight memory aid, which an autistic audience juggling several conversations may want.
18. **No reporting from inside a conversation history / no report categories beyond two** (`inappropriate`, `spam`) — limited taxonomy for a safety-first product.

~Product Designer

## QA Front-End Analyst

_Date: 2026-06-30_

POLL of MISSING / half-built **functional** items (absent or partial states, flows, and options that would frustrate users). Source-grounded; no live click-through this run (browsers read-tier / Chrome MCP denied — a visual mobile + MatchMoment pass is still owed). A11y and brand items excluded per brief.

**[Critical]**
1. **No client-side routing — Back/Forward and refresh lose all in-app state.** `App.jsx` is a single `activeTab` state machine; `history.replaceState` is used only to scrub `?verify`/`?reset` params, never to push tab/conversation state. Browser Back exits the app or reloads to the default Discover tab; an open conversation, the Matches deck position, and Settings are all unrecoverable. No deep-linkable URLs at all.
2. **Failed message sends silently vanish with no retry.** `ConversationScreen.handleSend` on a generic error removes the optimistic bubble and sets a transient status that auto-clears after 2s. The user's typed text is gone, there is no failed/queued state on the bubble, and no "Retry" affordance. Only 403/429 get sticky treatment; ordinary network failure loses the message.

**[High]**
3. **"No candidates nearby" vs "deck empty" are the same screen.** `SuggestionScreen` `atEnd` always renders "No new suggestions right now" regardless of cause — a brand-new user with zero candidates in radius, a user who exhausted a full deck, and a user whose filters/deal-breakers exclude everyone all see identical copy. No "widen your radius" vs "you've seen everyone" distinction, no refresh/reload-deck action (only "Go to Profile").
4. **No offline handling anywhere.** No `navigator.onLine` / online-offline listeners in the codebase. Losing connectivity surfaces only as generic per-screen "check your connection" errors after a request fails; no global banner, no send queue, no auto-retry on reconnect. Socket `connect_error` is swallowed silently, so real-time messaging can be dead with no user signal.
5. **MatchMoment "Say hello" dumps into the Messages list, not the thread.** `SuggestionScreen` `onOpenChat` calls `onOpenMessages()` which only switches the tab; `pendingConversationId` is never set (and for a brand-new match a conversation may not exist yet). The user lands on the inbox and must hunt for the person they just matched with. Known partial item — confirmed in source.
6. **No message pagination / history loading.** `getConversation` returns the full message set in one shot and renders all of it; there is no infinite scroll, "load earlier messages," or windowing. Long threads load everything at once (slow) and there is no way to page back if the server ever caps the payload.
7. **No delivery/read feedback on sent messages.** Optimistic bubbles show only a time-group label; there is no sending / sent / delivered / read state, no per-message timestamp, and `markConversationRead` updates the list dot but is never reflected back to the sender. Users can't tell if a message landed.

**[Medium]**
8. **Conversation "Archive" is a one-way trip — no archived view or restore.** `HeaderMenu` and the cap notice offer "Archive," and `archiveConversation` removes the row from the list, but there is no Archived section, no unarchive, and no way to find an archived thread again. The cap (5 active) forces archiving with no recovery path.
9. **No conversation search or filter.** `MatchesListScreen` renders flat Active/New sections with no search box, no filter, and no sort. Becomes unusable as matches accumulate.
10. **No profile preview ("see how others see me").** `ProfileScreen` is edit-only; there is no read-only render of the card a candidate sees in Discover, so users can't verify how their photos/prompts/bio actually present.
11. **Empty-input edge on starters + attachments dead code shipped.** The whole photo-attachment compose path (preview, upload, R2 intent, scan status) is built but gated off by `ATTACHMENTS_ENABLED = false`, while `sendMessage` still can't link an attachmentId — so even if flipped on, photos render only optimistically and never persist. Half-built feature carrying real surface area.
12. **No undo on the "Interested" choice.** Undo is offered only after Not-now/Skip (`SuggestionScreen`). An accidental "I'm interested" (which can trigger a real mutual match) has no take-back.
13. **MatchesScreen surfaces no empty/error distinction for the createConversation cap inline.** Cap-reached is a toast-style `error` string with no link/affordance to the archive action it tells the user to perform; they must navigate to Messages and find a row to archive themselves.

**[Low]**
14. **Verify-email banner "Resend" has no cooldown / rate feedback.** `VerifyEmailBanner` flips to "Sent" but allows immediate repeated resends before that; no throttle UI. (Backend email is also disabled live, so the banner path is effectively inert.)
15. **Auth form: no inline field validation or password-visibility toggle.** Errors are a single top-of-card alert; email format isn't validated client-side (`noValidate`), and there's no show/hide password control.
16. **Reaction cap is silent.** When a message hits `MAX_REACTION_TYPES` (5) the React (＋) button simply disappears with no explanation.
17. **No "new messages below" indicator.** The log force-scrolls to bottom on every change (`logRef.scrollTop = scrollHeight`); a user reading older messages gets yanked down with no jump-to-latest control or unread divider.

~QA Analyst

---

## Autonomous Build

### 2026-06-30 — Backlog item #1: Notifications / activity inbox

**What was built:** Activity inbox — a "Liked you" section inside the Matches screen showing people who swiped 'like' on you that you haven't acted on yet, plus a nav badge on the Matches tab.

**Backend** (`Spectrum-Dating-Server/src/routes/matching.js`):
- New `GET /matching/activity` endpoint. Returns `incomingLikes[]` (people who liked you, you haven't swiped on, no match exists) and `recentMatches[]` (mutual matches from last 7 days). Query uses existing `swipes`, `matches`, `profiles` tables — no migration needed. Each person includes `userId`, `displayName`, `age`, `photoUrl`, `pronouns`, `distCity`, `verified`, `likedAt`. Capped at 20 incoming likes. Live-verified: `{"incomingLikes":[],"recentMatches":[{"matchId":"…","displayName":"Eli Brenner",…}]}` ✅

**Frontend** (`src/api.js`, `src/MatchesScreen.jsx`, `src/App.jsx`):
- `api.js`: `getActivity()` → `GET /matching/activity`
- `MatchesScreen`: new `LikedYouSection` component — horizontal scroll of avatars with count badge, calm copy ("N people have expressed interest. Head to Discover…"), and a "Go to Discover to meet them" button. Accepts `onGoDiscover` + `onActivityCount` props (all hooks declared before early returns — no hook-after-return). Added "Your matches" subheading when mutual matches exist.
- `App.jsx`: `activityCount` state drives a badge on the Matches bottom-nav tab (cleared when entering Matches tab or signing out). `onGoDiscover` routes back to Discover.

**Deploy:** Backend health-gated deploy passed (5 checks). Frontend deployed to Vercel; alias `spectrum-dating-eta.vercel.app` re-pointed; live bundle `index-4V46myUf.js` contains `incomingLikes` / `Liked you` strings. ✅

**Verification:** Probed `GET /matching/activity` as sample user `mira.k.1` — 200 with correct shape. Build clean (90 modules). ✅

~Auto Builder
