# Status

## Frontend Dev

### 2026-06-29 — Responsive layout + navigation overhaul (width token, useViewport, mobile bottom-nav, desktop frame + 2-pane messages, Moderation moved to Profile)
- **One width system** — added `layout: { gutter: 20, maxContent: 640, maxForm: 400 }` to `src/tokens.js`. Replaced every per-screen content-column `maxWidth` (was 540/560/600/640) with `t.layout.maxContent` so there's no tab-to-tab width jitter: `App.jsx` (header inner + both verify banners), `SuggestionScreen`, `MatchesScreen`, `SafetyScreen`, `SettingsScreen`, `AdminScreen`, `ProfileScreen`, `messaging/MatchesListScreen` (×3), `messaging/BlockReportScreen`. `AuthScreen` routed through `t.layout.maxForm` (400); `OnboardingScreen` (480) and `LandingScreen` hero (760) left intentionally at their own widths. `UnmatchSheet` (540) left as a modal-sheet width (not a content shell).
- **Box-model guard** — `index.html` global `<style>` now has `*,*::before,*::after{box-sizing:border-box}`, `img,svg{max-width:100%}`, `body{overflow-x:hidden}`.
- **`src/useViewport.js`** — new hook returning `"mobile" | "tablet" | "desktop"` via `matchMedia` (desktop ≥1024, tablet 600–1023, else mobile), subscribed to media-query `change` events.
- **Navigation by device** — primary nav is ALWAYS the same 4 destinations (Discover · Matches · Messages · Profile), identical for admin and non-admin. On **mobile** the top tab bar is replaced by a FIXED bottom tab bar (`position:fixed; bottom:0`, 4 items ≥44px, icon+label, active=`t.accent`, `paddingBottom: env(safe-area-inset-bottom)`, `role=tablist/tab` + `aria-selected` + focus rings); `<main>` gets `paddingBottom:64` so content clears it. On **tablet/desktop** the existing top tab bar is kept. New `BottomNavTab` component + small inline nav glyphs in `App.jsx` (HeartIcon reused for Matches).
- **Moderation moved off the primary nav** — removed the admin-only "Moderation" top tab. `ProfileScreen` now takes `isAdmin` + `onOpenModeration` and renders a "⚙ Moderation" button near sign-out (new `ModerationButton`); App still renders `<AdminScreen/>` at `activeTab==='admin'`, now reached via Profile. Result: every user sees the identical 4-item nav. Admins can leave the admin view via the still-rendered top bar (desktop/tablet) or bottom bar (mobile).
- **Desktop/tablet frame** — on `!isMobile`, `<main>` becomes a centered surface "panel" (`background:t.surface; border:1px solid t.border; borderRadius:16; margin:24px auto`), capped at `maxContent+48` — reads as an app panel, not a strip adrift in grey. Mobile stays full-bleed.
- **Two-pane Messages on desktop** — `messaging/MessagingApp.jsx` reads `useViewport()`; on **desktop** it renders the conversation list (fixed 340px, `overflowY:auto`, right border) beside the open thread (flex) in a row, with the panel widened to 1080px. Empty thread pane shows a calm "Select a conversation" placeholder. Selected row is highlighted (`MatchesListScreen` threads a new optional `selectedConversationId` → `aria-current` + surfaceAlt bg); the thread's redundant "← Matches" back button is hidden via a new optional `hideBack` prop on `ConversationScreen`. Mobile/tablet keep the EXISTING list→conversation stack-swap unchanged.
- All hooks remain before any early return (`useViewport` added at the top of both `App` and `MessagingApp`; `hideBack` only gates JSX, not the `fBack` hook). `npm run build` clean (89 modules). Not deployed. ~Visual Dev

### 2026-06-29 — Animated "living logo" (AnimatedSpectrumMark)
- **New `src/AnimatedSpectrumMark.jsx`** — a calm animated version of the 6-tile brand mark. On mount the tiles **assemble** left→right: each tile fades + rises 6px + grows from `scale(0.86)`, staggered ~70ms apart, `t.motion.slow`(420ms)/`t.motion.gentle` easing, `animation-fill-mode: both` so each settles on the exact static `SpectrumMark` end-state (opacity 1, no transform). Opacity/transform-led, ≤6px travel — one-shot, never bouncy or looping-distracting. Same tile geometry/colours as `SpectrumMark` (square tiles, `height*0.16` gap, literal green→teal→sand hex). `transform-box: fill-box` + `transform-origin: center` so each tile scales in place. Props: `height` (default 18), `idle` (default **OFF**), `gap`, `radius`, `style`. Keyframes (`spectrumTileAssemble` + `spectrumTileBreath`) injected once via an id-guarded `<style>` (mirrors `Spectrum.jsx`'s loader pattern) — no animation libs.
- **Optional idle** (`idle` prop) — a *very* subtle slow brightness "breathing" sweep (`filter: brightness(1→1.06→1)`, ~6.5s `ease-in-out` infinite), staggered per-tile so it reads as a soft wave; starts only **after** the assembly fully settles (delayed by `assembleDone + i*stagger`) so entrance and idle never overlap. Default OFF; enabled only on the landing hero — a place where a quiet brand moment is appropriate. Header stays calm (idle OFF).
- **Reduced motion** — dynamic `usePrefersReduced` hook (same as `UnmatchSheet.jsx`/`MatchMoment.jsx`, with SSR/`matchMedia` guards). When reduced, the component returns the plain static `<SpectrumMark>` with zero motion (true static fallback, no chance of an unsettled frame). Belt-and-braces: even with the system pref off, App's global reduce-motion sheet (`animation-duration:0.001ms!important; animation-iteration-count:1!important`) collapses the keyframes to the settled `both`/end frames, so the mark renders fully visible and still.
- **Wired in** — replaced `<SpectrumMark height={14}/>` in the `App.jsx` header with `<AnimatedSpectrumMark height={14}/>` (idle OFF — header stays calm; removed the now-unused `SpectrumMark` import from App.jsx). Replaced the `LandingScreen.jsx` hero mark with `<AnimatedSpectrumMark height={36} idle/>` (larger + gentle idle for the brand moment); the serif "Spectrum" wordmark is unchanged. The landing **footer** keeps the static `SpectrumMark height={12}` (no need to animate a footer), so that import stays valid. `SpectrumMark.jsx` is untouched and remains the static fallback.
- `npm run build` clean (88 modules). Animated mark renders; reduced-motion shows the static mark. Not deployed. ~Brand Director

### 2026-06-29 — Round-3 QA: AllCaughtUp wired, empty-conversation illustration, error-state pattern + illustrations, favicon coverage, context-card removed from candidate cards
- **AllCaughtUp wired** — `src/SuggestionScreen.jsx` now imports `AllCaughtUp` and renders it (~110px) above the calm copy in the exhausted-deck / "No new suggestions right now." empty state (`atEnd` branch). The already-built illustration was previously exported but unused.
- **Empty-conversation illustration** — `src/messaging/EmptyConversationState.jsx` now imports `EmptyMessages` and places it (~96px) above the "You matched with {name}." heading, matching its sibling empty states. Starters logic untouched.
- **Illustration set completed** — `src/illustrations.jsx` gains two more soft 1.5px line-SVG glyphs in the existing style (`t.accent`/`t.clay`, no faces, `aria-hidden`): `GenericError` (a gently tilted rounded square with a quiet "!" resting inside) and `Offline` (a soft cloud with disconnected dashes drifting below).
- **Reusable `src/ErrorState.jsx`** — `ErrorState({ title = "Something went wrong", message, onRetry })`: centered, `role="alert"`, small `GenericError` (~80px) + serif title + `message` in `t.textSoft` + a secondary-variant `Button` "Try again" rendered only when `onRetry` is provided. Applied to the bare-text load-failure branches in: `src/MatchesScreen.jsx` (new `loadFailed` state + `loadMatches` retry; action-error inline `<p>` for CAP_REACHED etc. kept separate), `src/messaging/MatchesListScreen.jsx` (new `loadFailed`/`onRetry` props; `src/messaging/MessagingApp.jsx` now tracks `convsLoadFailed` and exposes a `retryLoadConversations` via `refreshKey` instead of swallowing the error), `src/messaging/ConversationScreen.jsx` (replaced the `apiError` one-liner; new `reloadKey` re-runs the load effect on retry), and `src/AdminScreen.jsx` (reports load-failure → `ErrorState` with `onRetry={refresh}`). No hook moved after an early return in any file.
- **Favicon coverage** — generated `public/favicon-32.png` + `public/favicon-16.png` from `public/icon.svg` (tile motif on `#3E6660`) via sharp. `index.html` now has `<link rel="icon" type="image/png" sizes="32x32"…>` + `sizes="16x16"`, keeps `<link rel="icon" href="/icon.svg">` as the modern default, and adds `<link rel="mask-icon" href="/icon.svg" color="#3E6660">` for Safari pinned tabs. (No `.ico` — PNG sizes + SVG cover the real cases.)
- **Context card removed from candidate cards** — `src/SuggestionScreen.jsx`: `CommStyleArea`/`commStyleChips` no longer render the free-text `contextCard` ("In their words" blockquote) to non-matched strangers (backend is being changed to stop sending it pre-match). The structured comms/sensory chips are kept. The `contextCard` still shows post-match (`MatchesScreen` / profile) — untouched. The queue still maps the field through harmlessly; nothing renders it pre-match.
- `npm run build` clean (87 modules); `favicon-32.png` + `favicon-16.png` present in `dist/`. Not deployed. ~Visual Dev

### 2026-06-29 — Signature "match moment" overlay (same-wavelength)
- **New `src/MatchMoment.jsx`** — a calm full-screen overlay for a NEW mutual match. Props `{ you:{name,userId,photoUrl}, them:{…}, onContinue, onOpenChat }`. Choreography is entirely opacity-led with short travel + soft deceleration (`t.motion.slow` / `t.motion.gentle`): (1) warm dim backdrop `rgba(36,51,45,0.55)` fades in; (2) two `<Avatar size={88}/>` (you left, them right) start ±24px apart (translateX only, no scale) and glide to a small gap; (3) the 6-colour brand spectrum ramp draws in left→right between them — each tile staggered ~60ms (base delay 220ms so the avatars have mostly met), fade + grow from 2px via `scale(0.25)→scale(1)`, like a connecting thread; (4) serif line **"You're on the same wavelength."** + subline "You both said yes." settle up (8px rise) at 360ms; (5) two reused `<Button>`s — **"Say hello"** (primary → `onOpenChat`) + **"Keep looking"** (tertiary → `onContinue`). NO confetti/bounce/sound. `entered` flips on next rAF to start the transitions; under reduced-motion (`usePrefersReduced`, same hook as UnmatchSheet) `entered` starts `true` so the END state renders immediately — avatars together, tiles drawn, text visible — with only the global fade, no movement.
- **A11y:** `role="dialog"` + `aria-modal` + `aria-labelledby` (heading) + `aria-describedby` (subline); heading is `tabIndex={-1}` and focused on open (announced); Escape → `onContinue`; Tab/Shift+Tab focus-trapped between the two buttons. Buttons keep their 44px targets + focus rings (Button's own ring); "Keep looking" text forced white for contrast on the dim backdrop.
- **`src/Button.jsx`** — wrapped in `forwardRef` (additive, ref forwarded to the underlying `<button>`) so MatchMoment's focus trap can reference the two actions. No existing call site changed; default export preserved.
- **Wired into `src/SuggestionScreen.jsx`** — imported `MatchMoment` + `getUserId`; new `getViewerIdentity()` helper builds `you` from the cached `spectrum_profile` (displayName/photoUrl) + `getUserId()`. New `matchId` state captured from the swipe result (`result.matchId`) alongside the existing `mutual` flag; reset in `next()`. Replaced the old in-card mutual-match interstitial with `<MatchMoment>` rendered as a fixed full-screen sibling when `stage === "confirmed" && mutual && lastPerson`: `them` = the just-liked candidate (displayName/memberId/photoUrl), `onOpenChat` advances past the moment then calls `onOpenMessages()` (lands them in Messages — the simplest existing pattern; `matchId` is captured for a future deep-link/createConversation flow), `onContinue` = `next()` (normal post-swipe advance). The non-mutual confirmed path and skip/not-now flows are untouched; `liveRef` is still used by the non-mutual block and its focus effect is null-guarded. ALL hooks remain before every early return — no hook moved.
- `npm run build` clean (86 modules). Not deployed. ~Visual Dev

### 2026-06-29 — Public landing page + calm empty-state illustrations
- **New `src/LandingScreen.jsx`** — the calm marketing front door (mobile-first, hero max-width ~760, sections centered). Hero: `<SpectrumMark height={32}/>` + "Spectrum" serif wordmark, headline "Dating at your own pace.", one-line subhead ("A calmer, clearer way to meet people — made with and for autistic adults."), and two buttons — **"Create your profile"** (primary → `onGetStarted`) + **"Sign in"** (tertiary → `onSignIn`). **"Why Spectrum is different"** — 4 value cards (white `t.surface`, soft `t.border`, `green50` icon chips) reusing `HeartIcon`/`GearIcon`/`ShieldIcon`/`SealCheckIcon` from `src/icons.jsx`: No pressure / Clarity over guesswork / Safety first / Built with the community. **"How it works"** — 3 steps (Make your profile → Discover people who fit → Talk when you're ready) each with a small 2×2 spectrum-tile motif (`StepTiles`, green/teal/clay) + number. **Closing CTA** — `green50` panel, repeats "Create your profile" with "Free to join. Leave whenever you like." **Footer** — muted mission line + "Your safety & privacy matter." note (no fake legal links/hotlines). All colours via `t.*` (auto-themes). Motion: a single one-shot fade on mount, neutralised by App's global reduce-motion stylesheet AND a scoped `@media (prefers-reduced-motion: reduce)` guard. Local `useFocusable` for focus rings; 52px primary/tertiary button targets.
- **`src/AuthScreen.jsx`** — added an optional `initialMode` prop (`"login"`|`"register"`, defaults `"login"`; seeds the existing `mode` state — login/register toggle behaviour untouched) and an optional `onBack` prop rendering a focusable "← Back" button above the wordmark (new `fBack` focusable). No behaviour regressed when props are omitted.
- **`src/App.jsx`** — new unauthenticated flow. Added `showAuth`/`authMode` state (declared with the other hooks, before any early return — no hook-after-return). A NEW visitor now sees `LandingScreen` first; `onGetStarted` → AuthScreen in **register** mode, `onSignIn` → AuthScreen in **sign-in** mode; AuthScreen's `onBack` returns to the landing page. Returning users with a stored token still skip straight to the app (the `authed` branch is unchanged and wins). Session-expiry routes to AuthScreen (login mode, `showAuth=true`) so the expiry message has context; sign-out and account-deletion reset `showAuth=false` to return to the calm landing page.
- **New `src/illustrations.jsx`** — 3 soft single-weight LINE SVG empty-state illustrations on transparent ground, `aria-hidden`, `size` prop (default 112), 1.5px stroke in `t.accent`/`t.clay` (CSS-var literals so they theme): `EmptyMatches` (two cups + steam), `EmptyMessages` (rounded speech shape + resting dots), `AllCaughtUp` (calm sun on a horizon). Wired `EmptyMatches` (size 104) into `src/MatchesScreen.jsx`'s empty state and `EmptyMessages` (size 104) into `src/messaging/MatchesListScreen.jsx`'s empty state, above the existing calm copy.
- WCAG 2.2 AA: decorative illustrations/marks `aria-hidden`; landing buttons keep ≥44px targets + visible focus rings; sections use semantic `header`/`section`/`ol`/`footer` with `aria-labelledby` headings; primary/tertiary use `t.accentStrong` for AA contrast; reduced-motion honoured. `npm run build` clean. Not deployed. ~Visual Dev

### 2026-06-29 — Visual polish (gradient-monogram avatars, skeleton loaders, button hierarchy)
- **Gradient-monogram avatars** — new shared `src/Avatar.jsx` exporting `Avatar({ name, userId, photoUrl, size=56, style })`. With a `photoUrl` it renders the image (circle, `object-fit: cover`); otherwise a deterministic two-tone diagonal gradient (`linear-gradient(135deg, …)`) derived from a stable string hash of the seed (`userId || name`) over a small palette of six calm brand pairs (greens→teals→sand/clay, literal hex so identical in both themes), with the initial centered in `t.serif`, white, `~size*0.42`. Visual is `aria-hidden` (surrounding context names the person). Adopted by replacing the per-screen monogram/avatar renderers: `SuggestionScreen.jsx` (removed local `Monogram`/`avatarBg`/palette; candidate lockup now `<Avatar userId={person.memberId} photoUrl size={88}/>`), `MatchesScreen.jsx` (removed local `Avatar`; `userId={otherUser.userId}`, size 56), `messaging/MatchesListScreen.jsx` (removed `SmallMonogram`/`avatarBg`/palette; `userId={otherUser.userId}`, size 44 via `AVATAR_SIZE`), and `ProfileScreen.jsx` (the photo-gallery EMPTY state now shows the member's own default gradient avatar at size 72 with calm "This is your default avatar…" copy — `PhotoGallery` takes a new `name={displayName}` prop). Photo-upload logic untouched — only the fallback visual changed.
- **Skeleton loaders** — new `src/Skeleton.jsx` exporting `Skeleton({ width, height, radius, style })`: a tinted block (`t.surfaceAlt` base, `t.green50` highlight) with a slow 2s shimmer via a one-time-injected `<style>` keyframe. Respects reduced motion — a `@media (prefers-reduced-motion: reduce)` rule drops the gradient + animation to a static tint. Replaced the "Loading…" text states with 2–3 calm skeleton rows/cards in `MatchesScreen.jsx` (`MatchesSkeleton`, 3 rows), `messaging/MatchesListScreen.jsx` (`MatchesListSkeleton`, 3 rows under the kept "Your matches" heading), and `AdminScreen.jsx` (`ReportsSkeleton`, 2 cards).
- **Button hierarchy** — new `src/Button.jsx` exporting `Button({ variant='primary', children, ...props })` with three calm variants: `primary` (solid `t.accentStrong`, white), `secondary` (`t.green100` fill, `t.accentStrong` text, 1px `t.border`), `tertiary` (transparent, `t.accentStrong` text). 44px min height, focus ring, 150ms ease transition; accepts `style` for width control (not forced full-width). Demonstrated in a few high-traffic spots only: `MatchesScreen.jsx` "Say hello"/"Open chat" action (primary / secondary by conversation state, removed its bespoke button), and `SafetyScreen.jsx` (← Back + elapsed-banner Dismiss as secondary, Share plan as primary). Did NOT refactor every button — the reusable component + these adoptions is the deliverable.
- WCAG AA: avatars/skeletons `aria-hidden`, decorative only; skeleton honours reduced-motion; Button keeps 44px targets + visible focus ring; all colors via `t.*` so both themes are covered. No hook moved after an early return. `npm run build` clean. Not deployed. ~Visual Dev

### 2026-06-29 — Visual identity phase 2 (warm dim theme via CSS variables + Settings theme toggle)
- **Approach — CSS variables, zero component churn.** Converted every COLOR token in `src/tokens.js` to reference a CSS custom property with the current light hex as the fallback, e.g. `bg: "var(--c-bg, #F4F5F2)"`, `text: "var(--c-text, #24332D)"`, `bgGradient: "var(--c-bg-gradient, linear-gradient(150deg, #F4F5F2 0%, #ECF0EB 100%))"`. Did this for all 32 color keys: bg/bg-gradient, surface, surfaceAlt, text, textSoft, textMuted, accent, accentStrong, positive, border, borderLight, focus, danger, warning, sand, clay, teal, the green50…green900 ramp, formBorder, bubbleOwn, bubbleOther, tombstone. `serif`/`sans` font strings left as literal strings (untouched). No key renamed or dropped — same keys, only values changed to `var(...)`. Components read `t.*` exactly as before and re-resolve live on theme switch.
- **`index.html` — both themes defined in the global `<style>`.** `:root` holds the LIGHT values, which EXACTLY match the current tokens.js hex (incl. the phase-1 nudged `--c-border:#D3DBD5`), so light mode is byte-for-byte unchanged. `:root[data-theme="dim"]` holds the warm-dim palette: deep warm-green surfaces (`--c-bg:#1C2422`, `--c-surface:#232D2A`, `--c-surfaceAlt:#283330`), lifted text (`--c-text:#E4EAE6`, `--c-textSoft:#B6C2BC`) and lifted accents (`--c-accent:#7FB0A7`, `--c-accentStrong:#8FBCB2`, `--c-positive:#7FB87A`, `--c-danger:#E08585`, `--c-warning:#D9B45A`) chosen to meet WCAG AA on the dim surfaces; the green ramp is inverted for dark (low end → dark panels, high end → light brand greens for text). `body { background }` now reads `var(--c-bg, …)`. Verified: identical 32-variable set in both themes; every var referenced in tokens.js is defined in both.
- **Theme persistence + live apply.** Extended the existing `spectrum_a11y` settings object with a `theme` field (`'light' | 'dim'`) rather than a separate key — keeps one settings blob. `readA11y()`/`DEFAULT_A11Y` in `SettingsScreen.jsx` updated to read/normalise it (defaults to `'light'`). `App.jsx`: new `applyTheme(prefs)` sets `document.documentElement.dataset.theme = prefs.theme === 'dim' ? 'dim' : ''`; called from both `applyA11y` (live, on toggle) and the existing `a11y` effect (covers initial mount + every change). The high-contrast `filter`/larger-text `zoom`/calm-background overrides are untouched and layer on top of either theme.
- **Settings UI.** Page H1 renamed "Accessibility" → "Settings" (the screen now covers both). New "Theme" section at the top: serif subhead + calm copy "Warm dim is easier on the eyes in low light." and a segmented control (`role="radiogroup"` / `role="radio"`, 44px targets, focus rings via `useFocusable`) with Light / Warm dim options; selecting one calls the same live-apply `update("theme", v)` path as the other settings. The four accessibility toggles sit under an "Accessibility" subhead below it, unchanged. No hook moved after an early return (this screen has a single return anyway).
- `npm run build` clean. Not deployed.

### 2026-06-29 — Visual identity phase 1 (Newsreader + Atkinson type, SVG icons, spectrum-tile mark, color-depth tokens)
- **Typography:** `index.html` now preconnects to Google Fonts and loads `Newsreader:opsz,wght@6..72,400;6..72,600;6..72,700` + `Atkinson+Hyperlegible:wght@400;700` (with `&display=swap`); added a global `body { font-family: 'Atkinson Hyperlegible', … }`. `src/tokens.js`: `serif` is now `'Newsreader', Georgia, 'Times New Roman', serif` and a new `sans: "'Atkinson Hyperlegible', -apple-system, Segoe UI, Roboto, sans-serif"` was ADDED. Replaced every hardcoded `fontFamily: "-apple-system, Segoe UI, Roboto, sans-serif"` (18 occurrences across 11 files) with `t.sans`. Headings already on `t.serif` pick up Newsreader automatically. The two files with their own LOCAL `t` objects (`OnboardingScreen.jsx`, `messaging/BlockReportScreen.jsx`) had `serif` updated + `sans` added locally so `t.sans` resolves there too. No token key removed — only values changed / keys added; verified every `t.sans` user has a `sans` source.
- **Iconography:** new `src/icons.jsx` exports small duotone-ish inline-SVG components (`ShieldIcon`, `GearIcon`, `HeartIcon`, `SealCheckIcon`) — 24-viewBox, 1.5px stroke, `currentColor`, low-opacity `currentColor` fill, `size` prop (default 18), `aria-hidden`. Replaced frontend emoji: `🛡`→`<ShieldIcon/>` and `⚙`→`<GearIcon/>` in `App.jsx` (Safety/Settings header links + the `⚙ Moderation` nav tab label, now `<><GearIcon/> Moderation</>`); `✓`→`<SealCheckIcon/>` in `VerifiedBadge.jsx`. (Reaction-picker emoji inside ConversationScreen and the push-payload emoji in the backend are out of scope.)
- **Spectrum-tile mark:** new `src/SpectrumMark.jsx` — decorative SVG of 6 rounded-rect tiles stepping green→teal→sand (`#5E9459 #4F8A8B #3E6660 #6FA39A #C9A875 #E7D9C4`), `height` prop (default 18, width auto), `aria-hidden`. Rendered at `height={14}` immediately before the "Spectrum" serif wordmark in the `App.jsx` header (wordmark kept). `public/icon.svg` updated: the plain serif "S" replaced with a centered row of 5 tiles in the same ramp on the `#3E6660` brand ground, keeping the 512 viewBox + `rx="112"` rounded corners (PNGs not regenerated — fine for now).
- **Color depth:** ADDED to `tokens.js` — `sand #E7D9C4`, `clay #C9A875`, `teal #4F8A8B`, and a coherent green ramp `green50 #EEF3F1 … green100 #DCE8E4, green200 #BCD4CC … green700 #3E6660 … green900 #24332D`. Nudged `border` from near-gray `#D8DDD6` to a subtly green-tinted `#D3DBD5` (mirrored in the two local-`t` files). Applied lightly: the quiet `SecondaryButton`s in `SafetyScreen.jsx` + `SettingsScreen.jsx` now use a `green100` fill instead of white; the Safety "What to say" script cards got a warm `sand` background + `clay` left-border accent. Conservative — the tokens being available is the main deliverable.
- `npm run build` clean. Not deployed.

### 2026-06-29 — Hinge-style profile prompts (editor + card display)
- `src/api.js`: added `getPromptCatalog()` → `GET /profile/prompt-catalog` (returns the `prompts` array, normalised) and `savePrompts(prompts)` → `PUT /profile/prompts` (returns the saved `prompts` array).
- **Item 1 — `src/ProfileScreen.jsx` (prompt editor):** new state `prompts` (`[{ promptKey, answer }]`, max 3), `promptCatalog` (`[{ key, text }]`), and `showPromptChooser` — all declared with the other `useState`s, BEFORE the loading/error early returns (no hook-after-return). A new own `useEffect` fetches `getPromptCatalog()` into `promptCatalog` (best-effort). The existing profile-load effect now maps `data.prompts` (server shape `{ promptKey, promptText, answer }`) into the editable `{ promptKey, answer }` shape, capped at 3. New calm **"Prompts"** card placed after "About you", before "Your interests", with the helper copy "Answer up to 3 prompts — an easy way to share who you are without a blank page." Each filled slot (`PromptSlot`) shows the prompt text + an editable answer textarea (≤200, live "remaining" counter) and a Remove control; an "Add a prompt" dashed button (`AddPromptButton`, shown when <3) opens a chooser (`PromptChooser`) — a `<select>` of catalog prompts NOT already chosen, then an answer textarea (≤200, counter), with Add/Cancel. `handleSave` now also calls `savePrompts(prompts.filter(p => p.promptKey && p.answer.trim()))` after `updateProfile` (best-effort; on failure surfaces "Your profile saved, but your prompts couldn't be saved."). Prompts are always saveable (the main Save button only gates on name + interests), so no extra `isDirty` plumbing was needed.
- **Item 2 — `src/SuggestionScreen.jsx` (candidate cards):** carried `prompts` through the candidate queue mapping (`c.prompts || []`). New `PromptCards` component renders each prompt Hinge-style — the `promptText` in a small muted uppercase label with the `answer` prominent in serif (20px) below — in its own quiet card after "How I communicate". Only rendered when the candidate has at least one answered prompt.
- **Item 3 — `src/MatchesScreen.jsx` (light):** when `otherUser.prompts` has an answered first prompt AND the row isn't already filled by a tagline/context card, show that first prompt's label + answer (single truncated serif line) as a calm preview under the name.
- WCAG 2.2 AA: answer textareas are labelled (visually-hidden label tied to the prompt in the editor) + `aria-describedby` to a polite live counter; Remove buttons carry a descriptive `aria-label`; the chooser select + answer are labelled; `useFocusable` focus rings and 44px targets throughout; prompt cards use semantic `ul`/`li`; colour is never the sole signal.
- `npm run build` clean. Not deployed.

### 2026-06-29 — Sensory / comms-style / context-card profile UI + candidate card display
- Surfaced the seven new "moat" profile dimensions (`commDirectness`, `commLiteral`, `commCadence`, `sensoryEnvironment`, `sensoryLighting`, `socialDuration`, `contextCard`) — they ride on the existing `getProfile`/`updateProfile` and appear on candidates + `match.otherUser`.
- **Item 1 — `src/ProfileScreen.jsx` (profile editing, end-to-end):** added all seven to `DEFAULT_PROFILE`; new `useState`s declared with the other hooks BEFORE the loading/error early returns (no hook-after-return — incl. a `contextCardTouched` flag for the counter). Populated from `data.*` in the load effect (each `|| ''`), wired into both `isDirty` branches + the effect dep array, the `savedProfile` snapshot, and the `updateProfile({...})` save payload. Two new calm cards placed after "Lifestyle", before "Notifications": (1) **"How you communicate"** — three `LifestyleSelect` dropdowns (Directness / Style / Reply pace) + the **"How to talk to me" context card** textarea (max 300, live "remaining" counter, helper copy as specified); (2) **"Sensory & environment"** — three `LifestyleSelect`s (Preferred setting / Lighting / Social energy). Reused the existing `LifestyleSelect` and textarea/counter patterns; helper "This helps matches know how to talk with you. Optional."
- **Item 2 — `src/SuggestionScreen.jsx` (candidate cards):** carried the seven fields through the candidate queue mapping (`c.* || ''`). New `CommStyleArea` + `commStyleChips(person)` helper render a tidy, low-stimulation "How I communicate" area in its own quiet `surfaceAlt` card (after Interests/Why): a calm row of labelled chips for the set comms/sensory prefs (e.g. "Quiet settings", "Direct", "Replies once a day"), intentionally skipping the low-signal `either`/`whenever`/`medium` values to keep 2–4 chips. When `contextCard` is set, a short quoted `blockquote` under a small "In their words" label. The whole area renders only when something is set.
- **Item 3 — `src/MatchesScreen.jsx` (light):** when `otherUser.contextCard` is present, show it briefly as a single italic quoted line (truncated with ellipsis) under the tagline.
- WCAG 2.2 AA: selects keep `aria-describedby`; the context-card textarea is labelled + `aria-describedby` (hint + polite live counter); candidate chips are a labelled `role="list"`; colour is never the sole signal.
- `npm run build` clean. Not deployed.

### 2026-06-29 — Accessibility settings (reduce motion, high contrast, larger text, calm mode)
- New `src/SettingsScreen.jsx` — frontend-only, no backend. Same calm shell as `MatchesScreen`/`SafetyScreen` (`t.bgGradient`, max-width 600, serif H1 "Accessibility", `← Back` via `onBack` prop). Four labelled `role="switch"` toggles (reusing the `ProfileScreen` switch pattern: `aria-checked` + `aria-labelledby`), each with a one-line plain description. Exports `A11Y_KEY` (`spectrum_a11y`), `DEFAULT_A11Y`, and a `readA11y()` helper that reads + normalises the saved JSON to a fully-typed object (safe against missing/garbage values). Changes save to localStorage immediately and call the passed `onChange(next)` so App re-applies live. Calm mode also flips reduce-motion on (kept coherent so the switches reflect what's applied). CRITICAL: all hooks (`useState` lazy-init, mount-focus `useEffect`, `update` `useCallback`) declared before the single return — no hook-after-return.
- `src/App.jsx` low-risk global application (no `tokens.js` refactor):
  - New `a11y` state read once on mount via `readA11y()`; `applyA11y(prefs)` callback updates state + localStorage + re-applies the stylesheet; an effect re-applies on every `a11y` change.
  - **Reduce motion / calm** → inject/remove a single `<style id="a11y-overrides">` in `document.head` with `*, *::before, *::after { animation-duration: 0.001ms !important; animation-iteration-count: 1 !important; transition-duration: 0.001ms !important; scroll-behavior: auto !important; }` (helper `applyA11yStylesheet`, idempotent; removes the node when neither pref is on).
  - **High contrast / larger text / calm background** → inline style overrides on the top-level app container via `a11yWrapperStyle(prefs)`: `filter: contrast(1.15)` (global, safe), `zoom: 1.15` (enlarges proportionally — px inline styles won't scale via root font-size; fixed sheets/dialogs render relative to the zoomed container so they stay consistent), and flatten `background` to `t.bg` (drops the decorative gradient) in calm mode. Footer note points users to browser/device zoom if they need more.
  - Discreet "⚙ Settings" header link (new `SettingsLink` mirroring `SafetyLink`: focusable, 44px, `aria-label="Accessibility settings"`/`aria-current`) sits in a flex row beside "🛡 Safety" — visible on all main tabs, NOT a 5th nav tab. New `activeTab === "settings"` branch renders `<SettingsScreen onBack={() => setActiveTab(prevTab || "suggestions")} onChange={applyA11y} />`; link sets `prevTab` on navigate-in so Back returns sensibly. `<main>` aria-label ternary now includes "Accessibility settings".
- WCAG 2.2 AA: `useFocusable` focus rings, 44px targets, `role="switch"`/`aria-checked`/`aria-labelledby` on every toggle, plain-language descriptions; respects the user's explicit choice regardless of OS reduce-motion.
- `npm run build` clean. Not deployed.

### 2026-06-29 — Pause toggle (#8), undo last skip (#7), privacy reassurance (#9), your-reports status (#10)
- `src/api.js`: added `undoSkip()` → `POST /matching/undo-skip` and `getMyReports()` → `GET /messaging/my-reports` (normalises `{ reports: [...] }` or bare array). `paused` rides on the existing `getProfile`/`updateProfile`.
- **#8 Pause/snooze** (`src/ProfileScreen.jsx`): new `paused` state declared with the other `useState`s — BEFORE the loading/error early returns (no hook-after-return). Added to `DEFAULT_PROFILE`, populated from `data.paused` in the load effect, wired into both `isDirty` branches + dep array, the `savedProfile` snapshot, and the `updateProfile({...})` save payload. New calm "Pause my profile" card (after Notifications, before Identity verification) reusing the existing switch pattern via a new `PauseToggle` (`role="switch"`, `aria-checked`, `aria-labelledby`). When ON, shows the reassuring literal copy "Your profile is paused. You won't appear in Discover… Your matches and messages stay."
- **#7 Undo last skip** (`src/SuggestionScreen.jsx`): extracted the candidate fetch/map into a reusable `loadCandidates` `useCallback` (also resets `index` to 0); mount effect now calls it. New `undoing` state + `handleUndo()` calls `undoSkip()`, and on `ok` re-fetches the deck so the un-skipped person reappears; `ok:false` and network errors resolve quietly (just resume viewing). Added an "Undo" `ActionButton` beside "Next person" on the confirmation interstitial, shown only after a `not_now`/`skip` choice ("Undoing…" while in flight).
- **#9 Privacy reassurance** (`src/SafetyScreen.jsx`): new calm "Your privacy" section advertising the no-presence design ("We never show when you're online… never put on the spot to reply quickly.").
- **#10 Your reports** (`src/SafetyScreen.jsx`): `getMyReports()` fetched on mount via a properly-declared effect (`reports`/`reportsLoading`/`reportsError` state, mount-guarded with an `active` flag). New "Your reports" section: per-report `reportedName`, `reason`, formatted date, and a `StatusPill` (Open=warning, Reviewed=accent, Actioned=accentStrong, Dismissed=textMuted). Empty state "You haven't reported anyone."; reassurance note "When you report someone, our team reviews it. You'll see the status update here."; loading + error states.
- WCAG 2.2 AA throughout: `useFocusable` focus rings, 44px targets, `role="switch"`/`aria-checked`/`aria-labelledby` on toggles, status pills carry their meaning as text (colour is not the sole signal).
- `npm run build` clean.

### 2026-06-29 — Verified badge across cards + profile verification section
- New reusable `src/VerifiedBadge.jsx` — a small, calm "✓ Verified" pill (12px, `t.positive` text + border, transparent fill, `title`/`aria-label="Identity verified"`, accepts an optional `style` override). Exported for reuse.
- Badge shown next to the display name (only when the relevant `verified` is true) in: `src/SuggestionScreen.jsx` (candidate name lockup — `verified` now carried through the queue mapping from `c.verified`), `src/MatchesScreen.jsx` (match card — `match.otherUser.verified`), `src/messaging/MatchesListScreen.jsx` (conversation row — `otherUser.verified`, name wrapped so the badge doesn't get truncated), and `src/ProfileScreen.jsx` (next to the "Your profile" heading).
- `src/ProfileScreen.jsx`: new `verified` state declared with the other `useState`s — BEFORE the loading/error early returns (no hook-after-return) — populated from `data.verified` in the API load effect. New calm "Identity verification" card (after Notifications, before Save): if verified → "✓ Your identity is verified." (positive); else → literal "coming soon" explainer + a disabled-looking "Coming soon" tag, no dead button.
- WCAG 2.2 AA: badge is subtle, non-flashy, `t.positive`; decorative ✓ marked `aria-hidden`, meaning carried by `aria-label`.
- `npm run build` clean.

### 2026-06-29 — Safety Center (meeting tips, copy-able scripts, date-plan share, check-in timer)
- New `src/SafetyScreen.jsx` — entirely client-side, no backend calls. Same shell styling as `MatchesScreen.jsx` (`t.bgGradient`, max-width 600, serif H1 "Safety Center"). Five sections: (1) **Meeting safely** calm plain-bullet checklist; (2) **What to say** — five autism-friendly literal scripts (break / clarity / ending early / declining / sensory) each in a quoted card with a "Copy" button (`navigator.clipboard.writeText` + execCommand fallback, "Copied" confirmation via shared `aria-live` polite region); (3) **Share a date plan** — form (who / where / datetime-local / "check by" time) that builds a plain-text summary and calls `navigator.share({ text })` if available, else copies it ("Copied — paste it to a trusted person"); (4) **Check-in timer** — 1h/2h/3h presets plus a "by HH:MM" preset when the plan's check-by time is set; persists `{ endsAt }` in localStorage (`spectrum_safety_checkin`) so it survives reload; live `setInterval` countdown (cleared on unmount), "Cancel timer", and on-elapse a calm in-app `role="alert"` banner + a `new Notification(...)` only if permission is *already* granted (never prompts); (5) **If you need help** — generic non-region-specific note, no fabricated numbers. Copied the small `useFocusable` hook locally. CRITICAL: every hook (including the lazy-init localStorage state and all effects) is declared before any early return — no hook-after-return.
- `src/App.jsx`: discreet "🛡 Safety" header link (new `SafetyLink`, focusable, 44px, `aria-label`/`aria-current`) added in a flex row beside the "Spectrum" wordmark — visible on all main tabs, NOT a 5th nav tab. New `activeTab === "safety"` branch renders `<SafetyScreen onBack={() => setActiveTab(prevTab || "suggestions")} />`; link sets `prevTab` on navigate-in so Back returns sensibly. `<main>` aria-label ternary now includes "Safety Center". `ProfileScreen.jsx` untouched.
- `npm run build` clean.

### 2026-06-29 — Lifestyle attributes + deal-breaker toggles
- `src/ProfileScreen.jsx`: added six new fields end-to-end. State (declared with the other `useState`s, before the loading/error early returns — no hook-after-return): `wantsChildren` / `smoking` / `drinking` (strings, default `''`) and `dbWantsChildren` / `dbNonSmoker` / `dbMustBeLocal` (booleans, default `false`). Extended `DEFAULT_PROFILE`, the API load effect (maps `data.wantsChildren` etc., default `''`/`false`), the `isDirty` comparison + `savedProfile` snapshot, and the `updateProfile({...})` save payload to include all six.
- New "Lifestyle" card (after "About your search", before "Notifications"): three calm labelled `<select>`s — Do you want children? (Prefer not to say / Yes / No / Open to it), Smoking and Drinking (Prefer not to say / No / Sometimes / Yes) — each with optional/“shown on your profile” helper text. New reusable `LifestyleSelect` sub-component.
- "Deal-breakers" subsection within the same card: three `role="switch"` toggles (new `DealBreakerToggle`, reusing the existing `NotificationToggle` switch pattern) for `dbWantsChildren` / `dbNonSmoker` / `dbMustBeLocal`, with literal copy and the calm explainer "Deal-breakers hide people who clearly don't match. People who haven't said yet still show up."
- WCAG 2.2 AA: focus rings via `useFocusable`, 44px targets, `aria-checked` / `aria-labelledby` on switches, `aria-describedby` on selects.
- `npm run build` clean.

### 2026-06-29 — DOB onboarding gate, age on cards, contact-gating copy
- `src/OnboardingScreen.jsx`: added required **Date of birth** field to step 1 — native `<input type="date">`, labelled, helper "You must be 18 or older to use Spectrum Dating.", `max` attr = today minus 18 years. Client-side step-1 validation: required, parses/validates the date, computes age, blocks advancing with inline error "You must be 18 or older to use Spectrum Dating." when < 18. `dateOfBirth` ('YYYY-MM-DD') added to the final `updateProfile(...)` payload; server's 400 message already surfaces via existing `handleSave` (`e.message`). New pure helpers `maxDobToday()` / `ageFromDob()` (browser-side, not in a workflow). All hooks remain before the single return — no hook-after-return.
- `src/SuggestionScreen.jsx`: candidate name now renders age when present ("Name, 29"), guarded by `typeof person.age === "number"`.
- Contact-gating clarity (audit #6): reassurance line on onboarding final step ("You're in control of who can reach you…"); calm one-liner added to empty states in `src/MatchesScreen.jsx` and `src/messaging/MatchesListScreen.jsx` ("Only people you've both matched with can message you.").
- `npm run build` clean.

### 2026-06-29 — Profile photo gallery (up to 6, choose main)
- Added photo API helpers to `src/api.js` (`addProfilePhoto`, `setPrimaryPhoto`, `deleteProfilePhoto`), each returning the server's ordered `photos` array.
- Replaced the single-avatar `PhotoUpload` in `src/ProfileScreen.jsx` with a responsive `PhotoGallery` (3-col grid, up to 6 cells). Each photo: square cover image, "Main" badge on the primary, "Set as main" on non-primary, inline-confirmed "Remove" (danger outline). One "+ Add photo" tile when under 6; helper text + empty state. Friendly 503 handling ("Photo uploads aren't available right now").
- `photos` state populated from `GET /profile/me` (`data.photos`); old `photoUrl` state and `PhotoUpload` component removed. All hooks declared before the loading/error early returns.
- `npm run build` clean.

### 2026-06-29 — Admin moderation dashboard + report wiring
- Added moderation/admin API helpers to `src/api.js` (`getAdminMe`, `getAdminReports`, `resolveReport`, `suspendUser`, `getAdminStats`, `reportUser`).
- New `src/AdminScreen.jsx`: self-contained moderation dashboard — stats row, status-filter segmented control (Open / Reviewed / Actioned / Dismissed / All), report cards with resolve (Reviewed/Actioned/Dismissed + optional note) and inline-confirmed suspend/unsuspend. Loading + empty states, WCAG 2.2 AA focus rings, 44px targets, live regions.
- `src/App.jsx`: captures `isAdmin` from login response and `getProfile()` (persists across reload), resets it on sign-out / expiry / account deletion. Discreet "⚙ Moderation" nav tab rendered only for admins; non-admins never see it.
- Report wiring: messaging `BlockReportScreen` submit now also calls `reportUser(..., conversationId)` alongside the existing block; pre-match `ReportModal` in `SuggestionScreen.jsx` now calls `reportUser` (plus block so the candidate is hidden).
- `npm run build` clean.

~Frontend Dev

### 2026-06-29 — Visual round-2: motion tokens, Spectrum primitive, drift-island fix, OG/favicon/maskable
- **Motion language → tokens.** Added a `motion` object to `src/tokens.js` (`fast`/`base`/`slow` durations; `standard`/`exit`/`gentle` easings). Adopted it where transitions already existed: `src/Button.jsx` (background/color/border-color → `base standard`), `src/LandingScreen.jsx` (both CTAs), and every switch/segmented control in `src/SettingsScreen.jsx` + `src/ProfileScreen.jsx` (track → `base standard`, thumb travel → `base gentle`). No per-use reduce-motion gate needed — App.jsx's global `transition-duration:0.001ms !important` sheet neutralises them all; confirmed unbroken.
- **Spectrum primitive.** New `src/Spectrum.jsx` exporting `Spectrum({ variant, value, count, ... })` built from the 6-colour brand ramp (green→teal→sand). Variants: `progress` (lit tiles + 12% unlit), `loader` (3 staggered cross-fading tiles, ~1400ms loop, static under reduce-motion), `meter`, `divider`. Decorative by default (`aria-hidden`); accepts `aria-label`. Wired `variant="progress"` into `src/OnboardingScreen.jsx` as the step indicator (replaced the bare "Step N of 3" text — kept the SR live region + step logic). Wired `variant="loader"` into the MatchesScreen "Starting…" button state.
- **Drift islands (dim-mode bug).** Removed per-component hardcoded `const t = {…}` shadows (with `serif:"Georgia…"` and frozen light hex that never themed) from `src/messaging/EmptyConversationState.jsx` (the named bug), plus `src/OnboardingScreen.jsx`, `src/AuthScreen.jsx`, `src/messaging/UnmatchSheet.jsx`, `src/messaging/BlockReportScreen.jsx` — all now `import { t } from "tokens"`. Fixed a leftover literal `#D8DDD6` border → `t.border` in AuthScreen. Georgia now appears only in the legitimate `tokens.js` font-stack fallback. Left intentional literals untouched (Avatar gradients, index.html theme vars, `#fff`, shadow rgba, brand tile hex). NOTE: a few pre-existing token-equivalent literals remain in `ConversationScreen.jsx` loading/error one-liners (`#4E5F58`, `#B94040`) and error-panel bg literals (`#FDF2F2`, `#FFF5F5`) — out of this pass's scope.
- **OG / favicon / maskable.** New `scripts/gen-share-assets.mjs` (sharp, Windows paths) generates `public/og.png` (1200×630, tiles + serif "Spectrum" + tagline on brand bg gradient) and `public/icon-maskable-512.png` (tile motif in ~80% safe zone on `#3E6660`). `index.html`: added og:title/description/image/type/url, twitter:card=summary_large_image + twitter:title/description/image, and `<link rel="icon" href="/icon.svg">` (apple-touch-icon kept). `manifest.json`: existing PNG icons → `"purpose":"any"`, added the maskable PNG as `"purpose":"maskable"` (fixes Android clipping from the old `"any maskable"`).
- `npm run build` clean; og.png + icon-maskable-512.png present in `dist/`; manifest valid JSON.

~Visual Dev

## Accessibility Director

### 2026-06-30 — A11y audit (keyboard/focus, ARIA, contrast in both themes, motion, forms, landmarks, autism calm-by-design)

Standard: WCAG 2.2 AA + autism calm-by-design. Method: full source audit of `index.html`, `src/tokens.js`, and all core-flow + messaging components; contrast ratios computed numerically for every flagged pair (both themes). NOTE: live-site + dev-server tooling (Chrome MCP, Preview) were both permission-denied this run, so findings are from code + computed ratios, not runtime captures — visual focus-order spot-checks on the deployed site still owed and listed at the end.

**Headline: the warm-dim theme regresses contrast on every filled "primary" control.** The dim palette deliberately makes `accentStrong` (#8FBCB2) and `positive` (#7FB87A) *light* tints — they're meant as TEXT on dark surfaces (they pass 6–7:1 there). But many controls use them as a FILL behind WHITE text. White-on-#8FBCB2 = **2.10:1**, white-on-#7FB87A = **2.32:1** — both fail AA badly. The `accentFill`/`dangerFill` tokens were created to fix exactly this and the messaging layer adopted them, but the rest of the app did not. Light theme is unaffected (accentStrong #3E6660 = 6.41:1). Since default theme is light, this bites only users who opt into Warm dim — but that's the low-vision/photophobia/sensory cohort most likely to need it, so it's serious.

🔴 Blocker
- **Primary buttons unreadable in dim theme** · 1.4.3 Contrast · `src/Button.jsx:13` (`primary` variant, white on `t.accentStrong`), `src/LandingScreen.jsx:59` (`PrimaryButton`), `src/SafetyScreen.jsx:73` (`PrimaryButton`), `src/OnboardingScreen.jsx:850` (Continue/Save). White-on-#8FBCB2 = 2.10:1 in dim. Fix: introduce a `t.onAccentFill`-style usage — switch these fills from `accentStrong` to `accentFill` (dim #356962 → white = passes), or define a dedicated "button fill" token that is dark in BOTH themes. One Button.jsx change fixes the largest surface (MatchesScreen "Say hello", MatchMoment "Say hello").
- **"I'm interested" action button unreadable in dim** · 1.4.3 · `src/SuggestionScreen.jsx:73` (`interested` kind: white on `t.positive`). 2.32:1 in dim. This is the single most important button in the core Discover→match flow. Fix: use a dark-in-both-themes positive fill (add a `positiveFill` token mirroring `accentFill`, or render dark text on the light tint).
- **"Yes, unmatch" destructive button low-contrast in dim** · 1.4.3 · `src/messaging/UnmatchSheet.jsx:177` (`background: t.danger`, white text). Dim `danger` #E08585 + white = 2.67:1. `dangerFill` (#9E3B3B → white = 6.68:1) already exists for this; DeleteConfirmDialog uses it correctly — UnmatchSheet was missed. Fix: `t.danger` → `t.dangerFill` on the confirm button (line 177–178 background+border).

🟠 Serious
- **Selected interest chips white-on-tint in dim** · 1.4.3 · `src/OnboardingScreen.jsx:154` & `:393`, `src/SuggestionScreen.jsx:114` (shared-interest pill). White on `t.accentStrong` = 2.10:1 in dim; these carry real text. Fix: same accentFill swap, or dark text on the tint.
- **Loading/error text hardcoded `#4E5F58` — invisible in dim** · 1.4.3 + theming regression · `src/SuggestionScreen.jsx:637` and `:645` ("Finding people for you…" / load-error). #4E5F58 on dim bg #1C2422 = 2.34:1. These literals survived the drift-island cleanup. Fix: `t.textSoft`.
- **Consent-gate + attachment-error panels hardcoded `#FFF5F5`/`#FDF2F2`** · 1.4.3 · `src/messaging/ConversationScreen.jsx:1402` (consent alert bg) and `:1433` (attachment error bg). In dim these stay near-white and `t.danger` (#E08585) text on them = 2.50:1; also visually jarring (a white box in a dark UI). Fix: theme the panel bg (e.g. `t.surfaceAlt`) and use `t.danger` per theme, or a tokenized danger-surface.
- **Disabled "Sign in" button hardcoded `#4E5F58`** · 1.4.3 / theming · `src/AuthScreen.jsx:246` (`loading ? "#4E5F58"`). White text on it = 6.77:1 so it *reads*, but it won't theme (stays the same grey-green in dim) and is an un-tokenized literal. Fix: a tokenized disabled fill.
- **ReportModal "Submit report" hardcoded `#B94040`** · theming · `src/SuggestionScreen.jsx:402`. White on it = 5.43:1 (passes), but the literal won't shift to `dangerFill` in dim — inconsistent with the rest. Fix: `t.dangerFill`.

🟡 Moderate
- **`aria-current="true"` is an invalid token** · 4.1.2 Name/Role/Value · `src/messaging/MatchesListScreen.jsx:76` (selected conversation row). `aria-current` takes an enumerated token; `"true"` is technically allowed by the spec but the semantically correct value here is `"page"` (matches the nav pattern used everywhere else in App.jsx). Low impact but should be `"page"` for consistency and correct SR phrasing.
- **VerifiedBadge text fails AA in LIGHT theme** · 1.4.3 · `src/VerifiedBadge.jsx:20` (`t.positive` text, #5E9459 on white = 3.59:1; on surfaceAlt = 3.15:1). It's a real word ("Verified"), not pure decoration. Dim is fine (6.11:1). Fix: use `accentStrong`/a darker green for the label text in light, or treat the green as icon-only + darker text.
- **Desktop 2-pane: empty thread placeholder isn't programmatically tied to the list** · 3.2.3/predictability · `src/messaging/MessagingApp.jsx:182–197`. The right pane shows "Select a conversation to start reading." as plain muted text with no role; when a keyboard user activates a list row, focus moves into the thread via ConversationScreen's heading focus — good — but the empty placeholder itself isn't announced. Minor; consider `role="status"` or an `aria-live` note. Calm-by-design otherwise solid (stable two-pane, no surprise swap).
- **Reaction picker is `role="toolbar"` but has no roving-tabindex/arrow-key nav** · 4.1.2 · `src/messaging/ConversationScreen.jsx:71` (ReactionPicker). Each emoji is a normal button (44px, labelled — good) and Escape works, but `role="toolbar"` implies arrow-key navigation that isn't implemented; SR users will hear "toolbar" and expect it. Either implement arrow nav or drop to a plain labelled group.

⚪ Minor
- **Composer textarea has no visible label** · only `aria-label` · `src/messaging/ConversationScreen.jsx:1548`. Functional for SR; a persistent visible label/placeholder is present ("Write a message…") so acceptable. No action required — noted for completeness.
- **`zoom: 1.15` for "Larger text"** · `src/App.jsx:302`. `zoom` is non-standard (works in Chromium/WebKit, ignored in Firefox). The footer note points users to browser zoom as backup, which mitigates. Consider `transform: scale` or root `font-size` if Firefox parity matters; low priority.
- Hardcoded backdrop rgba (`rgba(36,51,45,…)`) in modals is intentional and fine (overlays, not text surfaces).

**PASSED / held up well:**
- Skip link (`index.html:123` + `App.jsx:52`) — pure-CSS `:focus` reveal, jumps to `<main id="main-content" tabIndex=-1>`. Solid.
- Landmarks + nav: single `<header>`, `<main aria-label>`, `<nav aria-label="Primary">` on BOTH top (desktop/tablet) and fixed bottom (mobile) bars; `aria-current="page"` on active tabs; identical 4-destination IA for all users. Stable, predictable — good autism design.
- `document.title` + polite SR announce on every tab change (`App.jsx:481`, `:710`). S4 holds.
- Touch targets: ≥44px on nav tabs, message ⋯/＋/send (send 48px), reaction pills, chips, switches — consistently met.
- Focus management in flows: Onboarding focuses first invalid field on failed Continue (`:665`); AuthScreen focuses the error (`:38`); every modal/dialog/sheet (Delete, Unmatch, MatchMoment, ReportModal) focuses its heading on open, traps Tab, and restores focus on close; ConversationScreen restores focus to composer after delete. Strong.
- Forms: labels tied via `htmlFor`/`id`, `aria-required`, `aria-invalid`, `aria-describedby` for hints + live counters, `role="alert"` on inline errors. Onboarding/Auth/Report all correct.
- Reduced motion: dual-gated (OS `prefers-reduced-motion` + in-app toggle), global `transition-duration:0.001ms` sheet, plus per-component static fallbacks (MatchMoment/UnmatchSheet/AnimatedSpectrumMark render end-state). No autoplay/looping motion except the opt-in landing idle breath. Calm-by-design intent met.
- Decorative SVGs (icons, SpectrumMark, illustrations, avatars, glyphs) consistently `aria-hidden`/`focusable=false` with meaning carried by adjacent labels. Images have alt (`Shared photo`, `Attached photo preview`).
- Messaging log is `role="log" aria-live="polite"`, labelled, with a keyboard scroll hint. Reaction pills carry full meaning in `aria-label` (colour never sole signal). Deleted messages announce as tombstones.
- Dim-theme TEXT contrast (where tokens are used as intended) is excellent: textSoft 8.63:1, textMuted 5.35:1, accentStrong-as-text 6.76:1. The regression is strictly fill+white-text controls, not body text.

**Regressions vs the "last deploy fixed" list:** the messaging layer's accentFill/dangerFill adoption is correct, but the SAME pattern was NOT propagated to Button.jsx, LandingScreen, SafetyScreen, Onboarding, SuggestionScreen, or UnmatchSheet — so the dim-theme contrast fix is only half-landed. Hardcoded `#4E5F58`/`#FFF5F5`/`#FDF2F2`/`#B94040` literals flagged in the prior drift-island pass still remain in SuggestionScreen + ConversationScreen.

**Top items for the PM to prioritize:**
1. Fix dim-theme button contrast app-wide (🔴) — start with `Button.jsx` primary variant + `SuggestionScreen` "I'm interested" + `UnmatchSheet` confirm. Use `accentFill`/`dangerFill` (and add a `positiveFill`). This is the biggest real-user impact and a half-finished prior fix.
2. Replace remaining hardcoded `#4E5F58`/`#FFF5F5`/`#FDF2F2` literals with tokens (🟠) — Suggestion loading/error + Conversation alert panels.
3. `aria-current="page"` on the selected conversation row + VerifiedBadge light-theme text contrast (🟡).
4. Owed follow-up: re-run a live keyboard-tab + dim-theme visual pass on the deployed site once Chrome/Preview access is granted (this run was code-only). Focus-order and `:focus` ring visibility should be confirmed on real focus, not computed styles.

~A11y Director
